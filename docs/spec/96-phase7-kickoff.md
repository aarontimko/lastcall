# Phase 7 Kickoff Prompt (operational artifact, not design)

**Rulings: FROZEN 2026-09-05 — adversarial design review done (`z_ignore/review-phase7/design-review.md`, F1–F18, seven gating, all folded below; verdict at the fold: safe to build); the four draft items ruled by the orchestrator under the sponsor's 2026-09-05 delegation (§10 "Phase 7 kickoff rulings"), Alts on record for the phase-close list.** Five design directions were ruled by the sponsor in conversation on 2026-09-05 before this draft existed (verbatim in `00-spec.md` §10 "2026-09-05 — Phase 7 pre-kickoff rulings"): flag scope (hunk from the diff, file from the nav; the flag stores the rendered hunk), the note widget (Enter sends, Alt-Enter / Shift-Enter newline), send = stage into the agent pane's input box without submitting, target = the one agent pane of the repo's workspace or a picker, and the gate amendment that pulls the herdr send into Phase 7. They are the build unless the design review shows one of them cannot stand; the ruling items at the end of this file are the questions the *draft* surfaced on top of them.

Phase 7 of the lastcall phased program. Phase 6 closed on 2026-09-05 (PR #6, merged `6d0725f`): draft roots reviewed hunk by hunk, the collapsed classes with on-demand expansion, one draw per pass, the debounce cap (Amendment v1.6, B12), the nav offset, tracing. Unit floor **425** (lastcall lib 167, bin 6, engine 218, testkit 34); every tier green on merged main (lint, unit, e2e 12 PTY + 33 snapshots, prepush).

**The spine of this phase is that lastcall writes into the working tree for the first time.** Every operation so far wrote only the private store and the ledger; `restore` writes the user's file, and invariant 4 (`00-spec.md` §2) says how: compare-and-swap against the content the rendered diff was computed from, refused with a re-render if the file moved. The second half — flag with note — already exists in the engine as a per-path mark (`Ops::flag`/`unflag`, `ops.rs:628–697`; A8 `scenario_a8_flag_with_note_retained_through_accept_all`; `Row.flag`, the nav's `⚑`, `status --json`'s `flag`) and grows into the thing the spec's first paragraph promises: flag the questionable hunk *and discuss it with the agent*. herdr v0.8.2 has the primitive for the discussion: `pane.send_text` writes raw bytes into a pane's PTY and appends nothing (verified on the release tag: `src/app/api/panes.rs` `handle_pane_send_text` → `runtime.try_send_bytes(Bytes::from(params.text))`; params `{pane_id, text}`), so several flags can be staged into the agent's input box and submitted once by the human. **Phase 7 is therefore: the guarded write (restore hunk / file / deletion, with the symlink and conflict rules), the per-hunk flag with its paste-ready export, the staged send through herdr with a picker when the target is ambiguous, and the two agenda items the Phase 6 close-out parked here (the help overlay's height before any key is added; the bench tier run per phase).**

**The reference implementations:** `crates/lastcall-engine/src/ops.rs::accept_hunk` (`:438`, the CAS shape and the `Refused` vocabulary restore mirrors), `Engine::accept_with` (`engine.rs:1083`, the op-then-rescan critical section restore copies), `crates/lastcall/src/tui/run.rs::spawn_accept` (`:477`, the off-UI-task effect shape), `crates/lastcall/src/tui/app.rs` `Confirm` (`:305`, the modal pattern the note widget and the picker reuse), `crates/lastcall/src/tui/herdr.rs::focus` (`:557`, the one-request herdr call `stage` copies), `crates/lastcall/tests/test_e2e_tui_pty.rs::pty_accept_loop_and_restart` (the PTY shape).

---

## Mission

Build Phase 7 per [`00-spec.md`](00-spec.md) §8 "Phase 7 — Restore + flag" (deliverable text, the five gate items as amended at this kickoff, and the kickoff agenda line are authoritative), against the frozen contracts §2 (invariants 2, 4), §6.2 (ledger; Amendment v1.7 proposed below), §6.3 (the ops table: restore hunk / file, flag), §6.4 (symlinks, in-progress operations), §6.7 (TUI), §5.4/§5.9 (herdr requests), and the scenarios `01-scenarios.md` A7, A8, C4, D2, D4, D11, plus the rulings frozen in this file. Read, in this order:

- `00-spec.md` §1 (the first paragraph: what a flag is for), §2, §6.2–6.4, §6.7, §6.8, §8 Phase 7, §10 from "2026-09-05 — Phase 6 checkpoint and close-out" to the end, §11 (the two restore residuals), Amendments.
- `01-scenarios.md` §A (A7, A8), §C (C4), §D (D2, D4, D11).
- `docs/dev/engine.md` ("Storage walkthrough", "Accepting through the engine", "The fail-open ladder"), `docs/dev/tui.md` ("Architecture", "The accept loop", "Keys", "Hit-testing", "Adding a widget, with a snapshot", "The PTY harness"), `docs/dev/testing.md` ("The e2e tier", "The real-herdr subset", "The consumed API surface"), `docs/dev/bench.md`.
- `crates/lastcall-engine/src/{ops.rs, ledger.rs, hunks.rs, scan.rs, store.rs, engine.rs, status.rs, herdr/wire.rs, herdr/transport.rs}`; `crates/lastcall/src/tui/{app.rs, input.rs, render.rs, run.rs, herdr.rs, term.rs}`; `crates/lastcall-testkit/src/{herdr_schema.rs, herdr_spawn.rs, pty_tui.rs, fixture_parent.rs}`; the tests named above.

## Entry baseline (inherited obligations)

- **Unit-test floor: 425** (`just test-unit` at Phase 6 close: lastcall lib 167, bin 6, engine 218, testkit 34; §10). The suite only grows; a shrinking count at any commit is a verifier failure class. Other tiers at close: e2e 33 snapshot scenes + 12 PTY scenes (1 pre-existing ignore each), integration 61 tests across 13 binaries, real-herdr 4, prepush 64-case proptests, the status golden.
- **§8 Phase 7 kickoff agenda (Phase 6 close-out, §10 2026-09-05):** the help overlay is 31 rows after `expand` and already starts on the header row at 30 lines — the flag and restore keys need a scrolling or two-column overlay *before* they are added (deliverable 8, first TUI step); run the bench tier per phase (deliverable 14). The binary byte-size question is ruled out (§10 2026-09-05 close-out rulings) and is not a Phase 7 item.
- **§11 restore residuals** are already on the ledger and stay there: the hash-then-rename window (`00-spec.md:717`) and the orphaned-descriptor case (`:729`). Deliverable 1 implements exactly the mitigation those entries name (re-hash immediately before the rename; rescan after every restore) and nothing stronger.
- **Amendment A3 (2026-09-03): accept-hunk has no live CAS** (`ops.rs:416–437`, the reasoning in place). Restore is the opposite case by construction — it writes the live file, so the live CAS is the whole guard — and the kickoff says so where the two sit side by side (deliverable 1) so no reader "harmonises" them.
- **§3.5 circle-backs:** none is scheduled for Phase 7.
- **§6.1 is not amended by this phase.** No new config key: the keys are `[keys]` names with defaults (deliverable 9), the export directory is derived from the state dir, and the herdr behaviour keys off the existing `[herdr]` table.

## Entry preconditions (orchestrator-confirmed before launch)

1. Toolchain via rustup (`just toolchain`); cargo only through `just …` / `just cargo …`. The pre-commit hook runs fmt, clippy `-D warnings`, `check --no-default-features` and the unit tier (about a minute).
2. Your worktree branch was created from `main` and is locked to `.claude/worktrees/…`. **First action:** `git merge --ff-only feat/phase7-restore-flag` so this kickoff is in your tree; if the tip has moved past your branch point, rebase your branch onto it before building.
3. Verified facts you may rely on (all at `6d0725f`):
   - **The rendered hunk math is already exact for restore.** `hunks::apply_hunks(baseline, hunks, selected)` (`hunks.rs:245`) splices exactly the selected hunks into the baseline; a row's live content equals `baseline ⊕ all hunks` when its `current` oid is what the hunks were computed from. Restoring hunk `k` is `apply_hunks(baseline, hunks, every **content** index but k)` — no reverse-apply, no new diff code — valid only under the live CAS. **The synthetic mode hunk is never passed to `apply_hunks` in either direction** (review F1: it is always last, `scan.rs:651–657`, and `apply_hunks` would splice its literal `mode 100755` bytes at offset 0); the mode is handled beside the bytes exactly as `accept_hunk` does (`ops.rs:487–507`): restoring the mode hunk chmods to `rendered.baseline_mode` and writes no bytes, restoring a content hunk keeps the live mode. A deletion row renders as one hunk (`ops.rs:459–463`) and a mode-only change as a synthetic `Hunk::mode_change` (`hunks.rs:45`, `is_mode_change` `:58`).
   - **What the store holds:** every current blob is written with `hash-object -w` (`store.rs:307–309`, `:361–395`), so the content a restore removes from disk stays addressable by its oid in the private store for as long as the store lives (the store never runs `gc`/`prune`, `store.rs:9`). `cat_blob`/`cat_blobs` (`:442`, `:448`) read them — **as canonical git blobs**: `hash-object -w` runs with cwd = root, so clean filters (`text=auto`, `filter=lfs`) have already run (§6.4, `store.rs:20–22`, D3) and a blob's bytes are not the worktree's bytes for a CRLF or LFS path (review F2). `Store::filemode()` (`:291`) says whether the root honours the executable bit.
   - **Symlinks are modelled as git does:** `lstat` everywhere (`scan.rs:12`, `store.rs:311–321`), mode `120000` = `Mode::Symlink` (`git.rs:157–158`), the blob is the link text (D2 `test_integration_scenarios_d.rs:39–63`, "the blob is the link target, not the pointee"). Nothing in the engine opens a worktree file for writing today; `nix` is a workspace dependency (`Cargo.toml:40`, features `signal`, `process`) used by the testkit only — `O_NOFOLLOW` needs `std::os::unix::fs::OpenOptionsExt::custom_flags`: the engine gains `nix = { workspace = true, features = ["fs", "signal"] }` and uses `nix::fcntl::OFlag::O_NOFOLLOW.bits()` together with `create_new(true)` for `O_CREAT|O_EXCL`; **never `libc`** (workspace rule, `Cargo.toml:44`, `pty_tui.rs:12`; review F3). Add `libc` to the `just lint` grep so the rule is mechanical.
   - **Conflict and in-progress state are on the row and the head state:** `Row.conflicted` from the user's `ls-files -u` (`scan.rs:307–327`, `:436`, `:478`); `HeadState.in_progress` (`headstate.rs:40`, `in_progress_of` `:146`) covers `MERGE_HEAD`, cherry-pick, revert, rebase. C4 (`test_integration_scenarios_c.rs:87`) already asserts the conflicted rows during and after a merge.
   - **Case-only deletion restore:** `scan::probe_case_insensitive(root)` (`scan.rs:205`) exists and `Inputs.case_insensitive` (`:176`) feeds the scan; D4 is macOS-only. A case-sensitive directory listing is `read_dir` compared byte-for-byte, never `stat`/`exists` (which follow the filesystem's folding).
   - **The accept path is the template:** `AcceptRequest` (`engine.rs:291–304`), `Engine::accept_with` (`:1083–1122`: the op and the rescan in one critical section; an `Err` reloads the ledger from disk), `Accepted { outcome, seq, pile }`, `Refused` (`ops.rs:84–132`: `Moved`, `BaselineMoved`, `Unhashable`, `StillPresent`, `NonUtf8Path`, `NoSuchHunk`), `Rendered::of(row)` (`:71`). The TUI's `Effect::Accept(Vec<(PathBuf, AcceptRequest)>)` → `spawn_accept` (`run.rs:477`) → `Local::Accepted` → `AcceptFailed::of` (`app.rs:340–356`, matched on the typed error) → status line.
   - **The flag as it exists:** `Ops::flag(path, note)` / `unflag(path)` (`ops.rs:628–697`) write `Override.flag: Option<Flag { note, created_at }>` (`Flag` `ledger.rs:125`, `Override` `:133`; `is_empty` `:156`); a flag-only override has no `blob` field; accept-all keeps flags (`accept_all`, A8); `Row.flag` (`scan.rs`), the nav marker `⚑` (`render.rs:685–686`), the diff header's `⚑ <note>` (`render.rs:869`), `status --json` `FlagStatus { note }` (`status.rs:56–110`, human rendering `:333`). `SCHEMA_VERSION` is `"1.0"` (`ledger.rs:31`); only an unknown **major** moves the file aside (`parse()` `:304–318`), so a `1.1` file is readable by a `1.0` reader — **readable but lossy** (`Override` has `#[serde(default)]` fields and no `deny_unknown_fields`, `:133–148`; `from_wire` drops unknown fields and `to_wire` re-stamps the version it read) — which is why deliverable 3 writes both fields.
   - **The keymap:** `input::DEFAULT_KEYMAP` (`input.rs:96`) is the help overlay's order; `Action` (`:32`), the name → action table (`:158`), `Action::describe` (`:186`), `MODAL_KEYS` (`:123`, `confirm` = `y`/`enter`, `cancel` = `n`/`esc`), `modal_action` (`:129`). Free default letters include `c i m s t u v x y z`; `f` is `toggle_full_paths`, `e` is `expand`, `d`/`g`/`w` are herdr. `term::enter` leaves bracketed paste **off** (`term.rs:4`, `:79`) and no keyboard-enhancement flags are pushed, so Shift-Enter arrives as plain Enter in every terminal without the kitty protocol — Alt-Enter arrives as `Esc` + `Enter` (crossterm reports it with `KeyModifiers::ALT`) in all of them.
   - **The help overlay:** `render_help` (`render.rs:1171–1217`) lays out `keymap rows + MODAL_KEYS + blank + SELECT_NOTE`, height `rows + 4` clamped to the area, one column, `any key closes` only when there is a spare row; `tui_help_overlay` at 100×30 already starts on row 0. The confirm modal (`render_confirm`, `:1223–1273`) is the pattern for a centred, `Clear`-backed box with fixed keys.
   - **herdr:** `Transport::request(method, params)` (`transport.rs:93`) is one connection, one line out, one line in; `tui::herdr::focus` (`herdr.rs:557–570`) is the shape of a one-request call from the loop (`Effect::Focus` → `run.rs:646`); `wire::method` (`wire.rs:35–44`) lists the ten methods and `herdr_schema::CONSUMED_METHODS` (`herdr_schema.rs:35–46`) pins each with its result const — adding a method means adding it there and regenerating `consumed-surface.json` with `just herdr-schema-fixture` from the **pinned release asset** (never master, never by hand), then `just test-integration-herdr`. `derive` (`herdr.rs:358–390`) walks `cache.panes`, keeps agent-bearing panes whose `foreground_cwd`/`cwd` falls under a root, and records only the max-attention pane per root (`RootAgents.pane`); the picker needs every candidate, so it is a sibling walk, not a change to `derive`. `PaneInfo` (`wire.rs:215–240`) carries `pane_id`, `workspace_id`, `agent`, `display_agent`, `label`, `cwd`, `foreground_cwd`. The real-herdr tier (`herdr_spawn.rs`, `test_integration_herdr_real.rs`) spawns the pinned binary in isolation and self-skips without `LASTCALL_TEST_HERDR_BIN`; the spawned server hosts a shell pane a test can `pane.read` (`tab.create` at `test_integration_herdr_real.rs:417`).
   - **`pane.send_text` semantics (v0.8.2 tag):** raw bytes to the pane's PTY, no trailing newline, error `pane_not_found` or `pane_send_failed`; `agent.prompt` is the one-shot alternative (types, then Enter 300 ms later; `wait` verifies the occupant; §5.4). Whether the agent in the pane treats a newline byte as *submit* is the agent's business: Claude Code submits on Enter and accepts a multi-line **bracketed paste** (`ESC [ 200 ~` … `ESC [ 201 ~`) as one input; the markers are interpreted by the application reading the tty (zle, readline, Claude Code's input widget), never by the line discipline — hence deliverable 6's zsh proof and the sponsor round trip.
   - **Testkit:** `PtyTui::send(bytes)` (`pty_tui.rs:447`) types into the binary; `screen_text`/`rows`/`wait_for_text`; `fixture_parent::build` is untouched (three roots, the assertion at `fixture_parent.rs:82–88`); the Phase 6 opt-in helpers (`add_draft_root`, `draft_config`, `write_draft_config`) are the precedent for a scene-owned addition. `Fresh` in the engine scenario tests (`tests/common`) has `accept_file`/`accept_all`/`row`/`ledger`/`store` helpers to extend with `restore_*`.

## Deliverables

**Engine half (1–7), then the checkpoint, then the TUI half (8–13), docs and bench last (14).**

1. **Restore under compare-and-swap (engine; gate items 1, 2, 4).** `Ops::restore_hunk(rendered, hunks, k, fault)`, `Ops::restore_file(rendered, fault)`, `Ops::restore_deletion(rendered, fault)` → `Result<Outcome, OpsError>`, plus `RestoreRequest { Hunk { rendered, hunks, index }, File(Rendered) }` (a deletion row, `rendered.oid == None`, routes to `restore_deletion` like accept) and `Engine::restore(root, req) -> Result<Restored { outcome, seq, pile }, EngineError>` with the same op-then-rescan critical section as `accept_with` (`engine.rs:1083`). Rules, each its own unit or scenario test:
   - *Live CAS, twice:* refuse `Refused::Moved` unless `store.hash_path(path)` equals `(rendered.oid, rendered.mode)` at entry **and again immediately before the rename** (§6.3 write strategy; the window between the second hash and the rename is the §11 residual, nothing more). *Baseline CAS* as accept-hunk has it (`BaselineMoved`). *Conflicted* (`Row.conflicted`, or the root's `in_progress` is `Some` and the path is conflicted) → new `Refused::Conflicted { path }` (gate item 4; C4). *Non-UTF-8* as today.
   - *What is written:* hunk `k` → `apply_hunks(baseline, hunks, all content hunks but k)`; file → the baseline bytes (`Baseline::Present`); `Baseline::Empty` → a zero-byte file with the baseline mode, never a removal (F17); when the baseline is **absent** (an added file), the file is removed (ruling item 2 governs the confirm); mode-only hunk → `set_permissions` to the baseline mode when `filemode()`, no content write; baseline symlink → `unlink(path)` + `symlink(baseline link text, path)` whatever the live side is (a live directory already fails the CAS via `Unhashable("typechange")`); baseline regular file with a live symlink → the temp-file-and-rename path below, which replaces the link without following it (F10).
   - *Bytes on disk go through git's smudge/eol conversion (F2):* on a git root the canonical content (`apply_hunks` output or the baseline blob) is written to the store with `hash_bytes` and materialised with `git cat-file --filters --path=<root-relative> <oid>` run exactly as `hash_path` runs `hash-object` (the store's git, cwd = root, `GIT_WORK_TREE=<root>`), so a `text=auto` CRLF file comes back CRLF. Before that, `git check-attr filter -- <rel>` (read-only; add it to the store wrapper's allowlist) — a path with any `filter` attribute set (LFS, or a user filter the store cannot run) is refused with `Refused::Unhashable { reason: "filter=<name>; not restored" }` rather than risk writing a pointer; §11 records it (the orchestrator's docs commit). Draft roots (raw-byte content model) write the bytes as they are. Test `scenario_d3_crlf_restore_keeps_crlf` (baseline CRLF file under `* text=auto`, one-line edit, restore the hunk, the file on disk is byte-equal to the original CRLF bytes) and `ops_restore_refuses_a_filtered_path` (a `filter=foo` attribute → refused, file untouched).
   - *How it is written:* a temp file `.<name>.lastcall-restore-<pid>-<n>` in the **same directory**, created with `O_CREAT | O_EXCL | O_NOFOLLOW`, mode copied from the baseline (executable bit only when `filemode()`), bytes written and `sync_all`'d, then the second hash of the *live path*, then `rename(temp, path)` — atomic, and it replaces a symlink at `path` with the file rather than following it. On any error after the temp file exists, remove the temp file; never leave one behind (test). *Ghosts (F8):* the scan drops any path whose basename matches `.*.lastcall-restore-*` (one engine constant `ops::RESTORE_TEMP_GLOB` shared by `scan` and `ops`; test `scan_ignores_restore_temp_files`), so a second process scanning mid-write never shows the temp file as an added row; before creating its temp file a restore removes any stale `.<name>.lastcall-restore-<pid>-*` in the parent whose pid is not alive (`nix::sys::signal::kill(pid, None)` → `ESRCH`), so a crash never leaves a ghost past the next restore of that name (§11 gains the residual for a crash with no later restore). *Parents (F9):* before the first CAS hash every component of `path`'s parent chain is `lstat`ed from the root down; a symlink anywhere in it refuses `Refused::Unhashable { reason: "parent is a symlink" }` (test `ops_restore_refuses_a_symlinked_parent_directory`) — `O_NOFOLLOW` on the leaf alone would let a swapped parent carry the write outside the tree.
   - *Deletion restore:* the parent's `read_dir` must contain **no entry equal to the name byte-for-byte, and — on a root where `Inputs.case_insensitive` is true — no entry equal under case folding either** (F6: a byte-only rule would let `rename(temp, "f1")` fold onto an existing `F1` on APFS and *clobber* the case-renamed file); either match is `Refused::StillPresent { path }` with the colliding entry named in `Display`. A missing parent is recreated with `create_dir` component by component under the same lstat rule (F9; test `ops_restore_deletion_recreates_a_missing_parent`). Then recreate from the baseline blob (through the filter path above) with the baseline mode (A7: byte-identical to the baseline blob, hash-compared; D4: `f1` → `F1` refused).
   - *The ledger is never written by a restore.* The baseline stays; the rescan shows what is left (a hunk restore leaves the other hunks pending; a file restore clears the row unless the override blob differs from the tree, which the next scan resolves as today).
   - *Tests:* `ops_restore_refuses_when_the_file_moved_and_leaves_it_untouched`; `scenario_a7_restore_deletion_recreates_the_baseline_blob` (hash compare); `scenario_d4_restore_deletion_refuses_on_a_case_collision` on the `Fresh` git fixture as `scenario_d4_case_only_rename` builds it (`f1` → `F1`), gated by `probe_case_insensitive` only — no `#[cfg(target_os)]`, so the skip line stays visible and a case-insensitive Linux mount is covered; `ops_restore_hunk_on_a_row_with_a_mode_hunk_writes_no_mode_bytes` (two content hunks + `chmod +x`; restore hunk 0; the file equals `baseline ⊕ hunk 1`, still executable); `scenario_d2_symlink_restore_never_writes_through_the_link` (D2's E half: repoint, restore, link points back, the pointee's bytes **and mtime** unchanged; plus a baseline-regular-file/current-symlink case: the link is replaced by the file, the pointee untouched); `ops_restore_hunk_leaves_the_other_hunks_pending`; `ops_restore_refuses_a_conflicted_path` (C4 shape); `ops_restore_mode_only_hunk_chmods_and_writes_no_bytes`; `ops_restore_removes_an_added_file`; D11 (unicode + spaces) restores; a fault-injection test at a new `FaultPoint::AfterTempWrite` proving the temp file is gone and the live file untouched on failure.
2. **Gate item 1 — mid-review mutation (engine + PTY).** *Engine:* render (scan), mutate the file, `restore_hunk` → `Outcome.refused == [Moved{..}]`, the file's bytes equal the mutated bytes (byte compare), the returned pile shows the new delta. *PTY (deliverable 11):* the same through the binary with a second-shell write between the frame and the key; the status line reads `<path>: changed since rendered; not restored`.
3. **Amendment v1.7 — per-hunk flags (engine; pre-ruling 1).** `Override.flag: Option<Flag>` becomes `flags: Vec<Flag>` with `Flag { note, created_at, hunk: Option<FlagHunk> }`, `FlagHunk { index, header: String, text: String }` — the hunk header (`@@ -a,b +c,d @@`) and the rendered unified text (`Hunk.lines` with their tags) as they were on screen, so the export reproduces what the user flagged even after the file moves. `SCHEMA_VERSION` → `"1.1"`. **The writer emits both** `flag` (= `flags[0]` without its `hunk`, the 1.0 mirror; `null` when there are none) **and** `flags`; the reader takes `flags` when present, else lifts `flag` into a one-entry list; `Override::is_empty` becomes `blob.is_none() && flags.is_empty()`. Why both (F4): `from_wire` drops unknown fields and `to_wire` re-stamps the version it read, so a 1.0 binary opening a 1.1 file (merged `main` while this branch is open; a second machine sharing the state dir) would otherwise erase every flag on its first write — with the mirror it keeps the first; the loss of the rest under a downgrade is a §11 residual (orchestrator's docs commit). Tests: `ledger_1_0_flag_lifts_into_flags`, `ledger_1_1_round_trips_both_fields`, `ledger_1_1_with_flags_absent_lifts_flag` (the post-downgrade shape). `Ops::flag(path, note, hunk: Option<FlagHunk>)` appends; `unflag(path)` clears all of them (the TUI has no per-flag removal in Phase 7). `Row.flags: Vec<Flag>`; `status --json` keeps `flag` as the first entry (`status_version` stays 1, additive) and adds `flags: [{ note, created_at, hunk: { index, header } | null }, ..]` — the hunk text stays out of `status` (the export is its outlet); the golden gains `"flags": []` on every row (sanctioned regeneration, `just golden-update`, diff reported). A8 extended: two hunk flags on one file survive accept-all; a file flag has `hunk: None`. `00-spec.md` §6.2 and the amendment log are the orchestrator's; your report carries the proposal text.
4. **The paste-ready export (engine; gate item 3).** `flags::export(ctx: &ExportContext, path: &[u8], flag: &Flag) -> String` in the engine, deterministic, golden-tested (`crates/lastcall/tests/golden/flag_export.md`; regenerated only by a named `just` target). Shape (exact; the golden freezes it):
   ```
   lastcall flag · <root basename> · <root-relative path> · hunk 2 of 3 · 2026-09-05T18:04:00Z
   <attribution line when known: `last touched by <agent> · session <id>` — Phase 8 provides it; Phase 7 prints nothing>
   note: <the note, verbatim, every line>

   ```diff
   @@ -10,7 +10,8 @@
    context
   -old
   +new
   ```
   ```
   A file flag omits the `hunk n of m` segment and the diff block. One export per flag; a batch export (several flags on one file) is the exports joined by a blank line. Non-UTF-8 paths and lines render lossy with the same rule as `Refused`; **every byte below `0x20` other than `\n` and `\t` in a diff line renders in caret form (`^[` for ESC)** so a line containing a paste-end marker can never cut the bracketed paste short and submit the rest raw (F13; test `flags_export_escapes_control_bytes`). The golden is produced by an engine unit test with `FixedClock` and a fixed `ExportContext` (F12: the binary has no clock override); the PTY scene in deliverable 11 reads the export file, replaces the timestamp with `<T>` and the root basename with `<R>`, and compares to a second golden `flag_export_pty.md` regenerated by the same `just` target.
5. **Restore and flag through the TUI's engine seam (engine).** `Engine::flag(root, path, note, hunk) -> Result<Flagged { export: String, seq, pile }, EngineError>` (op + rescan like accept; the export is computed from the flag just written), `Engine::unflag`. `Restored`/`Flagged` are the loop's `Local` payloads.
6. **herdr staging (engine wire + TUI link; pre-rulings 3, 4).** `wire::method::PANE_SEND_TEXT`; `tui::herdr::stage(transport, pane_id, text) -> Result<(), String>` sends the export **wrapped in bracketed-paste markers** (`\x1b[200~` + text + `\x1b[201~`), no trailing newline, through `Transport::request`; `pane_not_found` → the status line says the pane is gone. `CONSUMED_METHODS` += `("pane.send_text", "ok")` (the schema's `success_response` const, shared with `agent.focus`; the array type becomes `; 11` and `testing.md`'s "ten methods" becomes eleven), fixture regenerated from the pinned asset, provenance updated. `tui::herdr::agents_for(cache, root) -> Vec<AgentCandidate { pane_id, label, status, workspace_label }>` — the same association walk as `derive` (`foreground_cwd`/`cwd` under the root), every agent-bearing pane **across every workspace** (F15: "exactly one" means one candidate overall; the candidate row shows its workspace; when the `w` scope is on, the walk narrows to the active workspace like the nav does), sorted by attention then label. **Proof:** the real tier already gets a shell pane from `tab.create` (`test_integration_herdr_real.rs:417`, `tab_created.root_pane.pane_id`), so hosting is settled. `herdr_real_send_text_lands_unsubmitted` starts an interactive **`zsh -f -i`** in that pane (`pane.send_text` of `exec zsh -f -i\r`; zle's `bracketed-paste` widget is on by default and holds a bracketed paste in the buffer — `cat` would not do, F7: the tty line discipline echoes and delivers each line regardless of the markers), waits for its prompt via `pane.read` (`source: "screen"`), stages `echo ONE\necho TWO` wrapped in the markers through `stage`, and asserts through `pane.read` that both lines sit on the prompt as pending input and **`ONE` never appears as command output**; then sends `\r` and asserts `ONE` and `TWO` are printed — the submit-once half. A mock-transport unit test pins the exact request `{method: "pane.send_text", params: {pane_id, text: "\x1b[200~…\x1b[201~"}}` regardless. The tier self-skips without `LASTCALL_TEST_HERDR_BIN` as today; the report says whether it ran.
7. **Checkpoint (sanctioned).** Report deliverables 1–6 with evidence (the tests by name, `just test-unit` per crate, `just test-integration` per binary, `just test-prepush`); the orchestrator runs the tiers and the first verifier before the TUI half starts. Do not begin deliverable 8 before the orchestrator relaunches you.
8. **Help overlay height (TUI; first TUI step, kickoff agenda).** `render_help` lays the rows out in **two columns** when `rows + 4 > area.height` and the width allows two `max_row_width + 3` columns; otherwise one column as today. The rows keep keymap order down the first column then the second; `SELECT_NOTE` and `any key closes` stay on their own full-width lines under the columns. `tui_help_overlay` is regenerated (sanctioned) and a new `tui_help_overlay_tall` at 100×45 shows the one-column form still holds. Unit test: 31 rows at 30 lines → two columns, every row visible; 31 rows at 45 lines → one column. `docs/dev/tui.md` names the two-column overlay as design-pass input.
9. **Restore keys and controls (TUI; ruling items 1, 2).** New keymap actions `restore` (default `u`) and `restore_file` (default `shift-u`), described `restore the hunk under the cursor` / `restore the selected file`, mirroring `accept`/`accept_file`: `u` on a content-hunk row in the diff → `Effect::Restore(vec![(root, RestoreRequest::Hunk{..})])`; `u` on a hunkless row (collapsed, binary) and `shift-u` anywhere → `RestoreRequest::File`; a **deletion row's single hunk is the file** (F16): `u` and `shift-u` on it both build `RestoreRequest::File` and both open the confirm (`Restore <path>? (deleted)`); a `[u restore]` control on the hunk header beside `[a accept]` and `[U restore file]` beside `[A accept file]` (hit targets `HunkRestore(i)`, `FileRestore`; `input_parity_restore` drives key and click to the same `App` and frame). The confirm modal (`Confirm.scope` becomes `ConfirmScope::{Accept(AcceptScope), Restore(RestoreScope)}`; `render_confirm` takes its title ` accept ` / ` restore ` and first row from the variant; `confirm_counts()` stays accept-only — F11) opens per ruling item 2 — the draft's Rec: before a **file** restore (`Restore <path> · N hunks?`) and before a restore that **deletes** an added file (`Delete <path>? (added since baseline)`); never for a hunk restore. `Local::Restored` → the status line names refusals with `not restored` wording (`Refused`'s `Display` gains the verb as a parameter or a sibling); a successful restore says `restored <path> hunk 2` / `restored <path>`; the selection advances like an accept does ("the pile visibly shrinks", §6.7). A restore never touches `App.expanded`; on a collapsed row it is whole-file.
10. **Flag with note, the note widget, the picker, the fallback (TUI; pre-rulings 1–4, ruling item 3).** New keymap actions `flag` (default `m`, `flag the hunk under the cursor (a file, from the nav) for the agent`) and `unflag` (default `shift-m`, `clear the selected file's flags`). `m` opens the **note modal** (`App.note: Option<NoteEntry { target: FlagTarget, text: String, cursor: usize }>`, rendered like the confirm box, title ` flag `, the target named on its first line — `f1 · hunk 2 of 3` or `f1 (file)` — then the text area, then the fixed keys line `⏎ send   Alt-⏎ newline   Esc cancel`): printable keys and Backspace edit, **Enter** sends. Newline: **Ctrl-J** in every terminal (byte `0x0A`, crossterm `Char('j')|CONTROL` in raw mode, `parse.rs:96–100`), plus **Alt-Enter** and **Shift-Enter** where the terminal reports them (Option-as-Meta, or the kitty protocol; F5: with no enhancement flags Shift-Enter is byte-identical to Enter and Alt-Enter is too unless Option is Meta, which Terminal.app and iTerm2 ship off — the sponsor's P2 intent is kept, the guaranteed key is Ctrl-J); the keys line reads `⏎ send   ^J newline   Esc cancel` and `docs/dev/tui.md` says why Shift-Enter cannot be promised. While the modal is open the loop enables bracketed paste (`EnableBracketedPaste` on open, `DisableBracketedPaste` on close; crossterm feature `bracketed-paste`) so a pasted multi-line note lands as one `Event::Paste` and inserts, never sends. Esc cancels; every other key is swallowed while the modal is open (as the help overlay does, `app.rs:1261`, `:1512`) except the non-printable `quit` keys (`ctrl-c`), which quit as the confirm lets them (F15); a click is ignored like under the confirm; `Resize`/`Tick`/`Herdr` events pass through. `NoteEntry.target` is `FlagTarget::{Hunk { root, path, rendered: Rendered, hunk: FlagHunk, of: usize }, File { root, path }}` **captured at `m` time from the row on screen**; Enter uses the captured hunk whatever a landing pile did to the row meanwhile (F14; test `app_note_modal_keeps_the_hunk_it_opened_on_across_a_pile`). An empty note is allowed (the flag is the message). Enter → `Effect::Flag { root, path, note, hunk: Option<FlagHunk> }` → `Engine::flag` off the UI task → `Local::Flagged { root, path, export, pile }`. Then the send, decided in the reducer from `HerdrView`: **exactly one** `agents_for` candidate → `Effect::Stage(pane_id, export)` and the status line `flagged f1 hunk 2 · staged to <agent label>`; **several** → the **picker modal** (`App.picker: Option<Picker { export, candidates, selected }>`, title ` send to `, one row per candidate `<label> · <workspace> · <status>`, ↑↓ / `j` `k` move, Enter stages to the selected one, Esc leaves the flag written and stages nothing: `flagged f1 hunk 2 · not sent`; the same `ctrl-c`/click/pass-through rules as the note modal; a `Herdr` update while it is open re-derives the candidates and clamps `selected`); **none, or no herdr link** → the export is appended to `<state_dir>/exports/<root basename>/<YYYY-MM-DD>.md` (created as needed, one file per root per day, each export followed by a blank line) and the status line reads `flagged f1 hunk 2 · export → <path>`. `shift-m` → `Effect::Unflag(root, path)`. The nav marker becomes `⚑` or `⚑2` when a file carries several flags; the diff view marks a flagged hunk's header with `⚑ <first line of the note>` on the right (the file-level `⚑ <note>` line at `render.rs:869` stays for file flags). A `Stage` failure (`pane_not_found`, transport error) reaches the status line as `flagged f1 hunk 2 · send failed: <reason>` — the flag is already on disk, so nothing is lost.
11. **PTY scenes (TUI; gate items 1, 2, 3).** `pty_restore_hunk_then_file_bytes_match_baseline` (two-hunk edit; `u` on hunk 1 → the file on disk equals `baseline ⊕ hunk 2` (byte compare from the test's own copy); `shift-u` + `y` → the file equals the committed bytes; the pile shrinks; `q` exits 0); `pty_restore_refused_when_the_file_moved` (deliverable 2's PTY half); `pty_restore_deletion_recreates_the_file` (delete a tracked file in the fixture, `shift-u` on the deleted row → the file is back, hash-equal); `pty_flag_note_exports_when_standalone` (`m`, type a two-line note with Alt-Enter, Enter → the export file exists under the isolated state dir and equals the golden's shape for that fixture; the nav shows `⚑`; a relaunch still shows it). No PTY scene talks to herdr.
12. **Snapshots (TUI).** New: `tui_note_modal`, `tui_agent_picker` (an `App` with a `HerdrView` carrying two candidates — build it from a scripted `Cache` as the Phase 5 snapshots do), `tui_restore_confirm`, `tui_diff_view_flagged_hunk`, `tui_nav_flag_counts`, `tui_help_overlay_tall`. Sanctioned regenerations: `tui_help_overlay` (two columns, the four new rows), `tui_accept_controls` (the `[u restore]` / `[U restore file]` controls), and every snapshot whose frame shows a hunk header with `[a accept]` — list each in the report; any other `.snap` change is a defect to revert and explain.
13. **Unit tests the reducer owes (TUI).** `app_restore_on_a_hunkless_row_is_the_whole_file`; `app_restore_file_asks_first_and_restore_hunk_does_not`; `app_restore_of_an_added_file_asks_with_the_delete_wording`; `app_flag_from_the_nav_carries_no_hunk_and_from_the_diff_carries_the_cursor_hunk`; `app_note_modal_enter_sends_ctrl_j_and_alt_enter_break_the_line_esc_cancels`; `app_note_modal_paste_event_inserts_and_never_sends`; `app_note_modal_swallows_keymap_keys_but_ctrl_c_quits`; `app_note_modal_keeps_the_hunk_it_opened_on_across_a_pile`; `app_restore_on_a_deletion_row_is_the_file_and_asks`; `app_flagged_with_one_agent_stages_without_asking`; `app_flagged_with_two_agents_opens_the_picker`; `app_flagged_with_no_link_writes_the_export_file` (through a temp state dir); `app_stage_failure_keeps_the_flag_and_names_the_reason`; `herdr_agents_for_lists_every_pane_under_the_root_sorted_by_attention`; `input_parity_restore`, `input_parity_flag` (the click on `[u restore]` / `[m flag]` — draw a `[m flag]` control on the hunk header too, so the mouse path exists per §6.7).
14. **Docs and bench.** `docs/dev/engine.md`: "Restore and flag (Phase 7)" (the CAS pair, the temp-file write, symlinks, deletion restore, the ledger untouched, `flags` v1.1, the export shape); `docs/dev/tui.md`: the new keys in the table, the note modal, the picker, the fallback file, the staged-send story (what the human still presses), the two-column overlay as design-pass input; `docs/dev/testing.md`: the export golden and its `just` target; `README.md` a "Flag and discuss" paragraph and the keys; **bench**: a release `just bench` run at the end, `docs/dev/bench.md` rows updated with before/after (Phase 6's numbers are the before), and a note if any row moved more than noise. `00-spec.md` §6.2 / §6.3 / §8 / §10 / §11 / Amendments are the orchestrator's at the docs commit, not the worker's.

**Worker split (ruling item 4):** one Opus 5 worker, deliverables in the order above, on the phase branch `feat/phase7-restore-flag`, with the sanctioned checkpoint at deliverable 7.

## Boundaries

- Never touch `~/.config/herdr/`, `~/.config/lastcall/`, `~/.local/state/lastcall/`, `z_ignore/`, or the reference clones `~/dev/git/herdr` and `~/dev/git/drydock`. Every test runs through an injected `Env` over a temp dir; the real-herdr tier only through `herdr_spawn`'s isolation.
- Never `git push`, never commit on `main`, never `git add -A`/`git add .`, never `rm -rf`. `cargo` only through `just`.
- Frozen contracts (§5, §6, `01-scenarios.md`) change only by an amendment proposal in your report; the one expected proposal is Amendment v1.7 (deliverable 3). The export shape and the `flags` field are the only additions to `status --json` (additive, `status_version` 1).
- Restore writes **only** the path it was asked for, only under the live CAS, only through the temp-file-and-rename path; no other code path in this phase opens a worktree file for writing. A restore never writes the ledger.
- Snapshot updates (`just snapshots-update`) only for the new scenes and the sanctioned regenerations named in deliverable 12; the status golden only by `just golden-update` for the `flags` field.
- `just test-prepush` before the checkpoint report and before the final report; the unit count in each report, by crate.

## Gate evidence forms (what the PR table will cite)

| Gate item (§8, as amended at this kickoff) | Evidence |
|---|---|
| Mid-review mutation: refusal + re-render, file untouched (byte-compare) | engine `ops_restore_refuses_when_the_file_moved_and_leaves_it_untouched`; PTY `pty_restore_refused_when_the_file_moved` |
| Restore of a pending deletion recreates the file identical to the baseline blob (hash compare) | `scenario_a7_restore_deletion_recreates_the_baseline_blob`; `scenario_d4_restore_deletion_refuses_on_a_case_collision`; PTY `pty_restore_deletion_recreates_the_file` |
| Flagged hunk exports path+hunk+note (golden); **[sponsor]** three flags staged into a real agent pane and submitted once (transcript); standalone fallback = the export file | goldens `flag_export.md` + `flag_export_pty.md`; A8 extended; `pty_flag_note_exports_when_standalone`; `herdr_real_send_text_lands_unsubmitted` (zsh, pending input then submit-once) + the mock-transport request test; the sponsor's transcript on the PR |
| Symlink restore never writes through the link; restore refused on conflicted paths | `scenario_d2_symlink_restore_never_writes_through_the_link` (pointee bytes + mtime); `ops_restore_refuses_a_symlinked_parent_directory`; `ops_restore_refuses_a_conflicted_path` (C4 shape) |
| Verifier run: "restore without CAS", "restore clobbers newer content", "restore follows symlink" (+ this kickoff adds "restore writes filtered bytes", "restore writes mode-hunk bytes", "flag export drift", "send to the wrong pane", "ledger 1.0 ↔ 1.1") | findings + resolutions in the PR |
| Agenda: help overlay height | the two-column unit test; `tui_help_overlay` + `tui_help_overlay_tall` |
| Agenda: bench per phase | `bench.md` rows with before/after |

**Design review fold (2026-09-05):** F1 mode-hunk bytes, F2 filtered bytes, F3 `libc`, F4 ledger downgrade loss, F5 newline keys + paste, F6 case-fold clobber, F7 the `cat` proof — all gating, all folded above; F8 temp-file ghosts, F9 symlinked parents, F10 symlink cases, F11 `ConfirmScope`, F12 golden timestamp, F13 export escapes, F14 stale flag target, F15 workspaces + modal exits, F16 deletion row, F17 `Baseline::Empty`, F18 citations — folded. Rejected: none. One fold touches a pre-ruling's mechanism, not its intent: P2's "Alt-Enter newline everywhere" is not deliverable on the sponsor's default terminals, so Ctrl-J is the guaranteed newline and Alt/Shift-Enter work where reported (Alt on record: push kitty enhancement flags for the modal's lifetime, guarded by `supports_keyboard_enhancement()`).

## Rulings

**Pre-ruled by the sponsor on 2026-09-05 (verbatim in §10 "Phase 7 pre-kickoff rulings"; the Alts stay on record):**

- **P1 Flag scope.** Rec: hunk from the diff view, whole file from the nav; the flag record stores the rendered hunk (Amendment v1.7, deliverable 3). Alt: one flag per path, a second replaces the first — no ledger change, but no queue on one file.
- **P2 Note entry.** Rec: Enter sends, Alt-Enter newline everywhere, Shift-Enter newline where the terminal reports it, Esc cancels (deliverable 10). Alt: single-line notes only in Phase 7.
- **P3 Send = stage, not submit.** Rec: `pane.send_text` in bracketed-paste markers, the human presses Enter in the agent pane once (deliverable 6). Alt: `agent.prompt` one-shot with `wait` — right when the agent should start now and herdr's occupant check and wait matter more than the queue.
- **P4 Target.** Rec: exactly one agent pane under the root sends silently; several → a picker; none/standalone → the export file (deliverable 10). Alt: always the picker.
- **P5 Roadmap.** Rec: the third Gate 7 item is amended at this kickoff to the staged-send round trip with the export file as the standalone fallback (§3.4; the orchestrator edits §8 at the kickoff docs commit; the PR ratifies). Alt: keep the gate as written and build the send unlisted.

**Surfaced by this draft — ruled by the orchestrator 2026-09-05 under the sponsor's delegation ("if most of the items are pretty easy two way door decisions … go ahead and do the recommendations"); every Rec below is the build, every Alt stays on record for the phase-close list:**

1. **Default keys for restore and flag.** Rec: `u` restore hunk, `shift-u` restore file (the `a` / `shift-a` pair, and "undo" is what a reviewer reaches for), `m` flag ("mark for the agent"), `shift-m` clear flags; all four `[keys]`-rebindable like every other action. Alt: `x` / `shift-x` for restore (an "x out" reading, keeps `u` free for a later undo of an accept) and `t` for flag ("tag") — right if the sponsor expects an accept-undo in Phase 8 and wants `u` for it.
2. **When a restore asks first.** Rec: a hunk restore never asks (one keypress, like accept; the CAS is the guard and the removed content stays in the private store by oid); a **file** restore asks (`Restore <path> · N hunks?`), and a restore that would **delete** an added file asks with the delete wording. Alt: every restore asks — right if the sponsor would rather pay a keypress per hunk than ever restore the wrong hunk in a long review; the modal is the same code either way.
3. **Where the standalone export goes.** Rec: `<state_dir>/exports/<root basename>/<date>.md`, appended, path in the status line — nothing new to configure, survives a restart, greppable; the clipboard arrives with Phase 8's select-to-copy. Alt: OSC 52 clipboard write from the TUI now — right if the sponsor runs lastcall standalone often enough that "open the export file" is a chore; it is a terminal-capability gamble (herdr, tmux and iTerm2 differ) that Phase 8 has to face anyway.
4. **Worker split.** Rec: one Opus 5 worker with the sanctioned checkpoint after deliverable 6 (the engine half is self-contained and the TUI half depends on `Engine::restore`/`flag` and the export). Alt: two workers on one branch (7a engine 1–6, 7b TUI 8–13 against a stubbed engine seam) — right if wall-clock matters more than the merge cost.
