# Phase 6 Kickoff Prompt (operational artifact, not design)

**Rulings: FROZEN 2026-09-05.** The adversarial design review ran on 2026-09-04 (15 findings, F1–F15, all folded here; seven were gating — a fixture change that would have broken the golden and every scene, two false cost claims, an expansion design with no home for its hunks, a discovering-frame plan resting on wrong exit codes, loop tests the loop could not host, and a §6.5 change without an amendment; record in `z_ignore/review-phase6/design-review.md`). The sponsor ruled Rec on all five items on 2026-09-05 (verbatim in `00-spec.md` §10 "2026-09-05 — Phase 6 kickoff rulings"): `e` expansion of collapsed rows, the 3 s debounce cap with Amendment v1.6, the Claude Design pass deferred to the Phase 9 kickoff, the stderr discovering line, one worker with a checkpoint after deliverable 5. Every "per the Rec" deliverable below is the build; every Alt is on record only.

Phase 6 of the lastcall phased program. Phase 5 closed on 2026-09-04 (PR #5, merged `839da62`): herdr in the UI, the bounded pool, the N-instance locks, the compat workflow, and the sponsor's live run in the recipe layout. Unit floor **390** (lastcall lib 146, bin 5, engine 205, testkit 34); every tier green on merged main.

**The spine of this phase is that most of its deliverable already exists.** The draft-root ledger and the collapsed classes were built into the engine during Phases 2–4 because the seen-tree model needed them from day one: `RootKind::Draft` roots are discovered (`roots.rs:192–235`), opened over a private store with no user repo and no alternates file (`Store::open(…, RootKind::Draft, …)`, `store.rs:7` and the assertion at `:965`; first sight at `engine.rs:1132`), follow `draft_initial` (`engine.rs:2373`), and survive a restart (`test_integration_scenarios_f.rs` F1–F3); `render_content` (`scan.rs:591–650`) decides `Collapsed::{Glob, Binary, Size}` and skips the hunk diff for collapsed rows (D7/D8 in `test_integration_scenarios_d.rs:181–225`); the nav marks a collapsed row `⊟` (`render.rs:663`), the diff view shows `collapsed (<kind>) · +a −d · expands in a later phase` (`render.rs:884–896`, snapshot `tui_diff_view_collapsed`), and `a`/`A` on a hunkless row accepts the whole row (`render.rs:353`, `app.rs:640`). **Phase 6 is therefore three things: the gate evidence in the exact forms §8 names, the gaps those forms expose (a draft root reviewed hunk by hunk through the TUI; the lockfile and binary/oversize nav frames; the placeholder text), and the agenda the last two close-outs parked here** (§10 2026-09-04, both entries): the TUI loop's draw discipline, the blank seconds before the first frame, tracing where the diagnosis needed it, the nav's scroll offset, one remote-ref listing per scan, and the debounce question.

**The reference implementations:** `crates/lastcall/tests/test_e2e_tui_pty.rs::pty_accept_loop_and_restart` (`:656`, the restart shape gate item 1 reuses), `crates/lastcall/tests/test_e2e_tui_snapshots.rs::tui_diff_view_collapsed` (`:321`, the snapshot shape gate items 2 and 3 reuse), `crates/lastcall-testkit/src/fixture_parent.rs` (the `notes` draft root every TUI scene already carries).

---

## Mission

Build Phase 6 per [`00-spec.md`](00-spec.md) §8 "Phase 6 — Draft dirs + collapsed classes" (deliverable text, the three gate items and the kickoff agenda line are authoritative), against the frozen contracts §6.1 (`draft_dirs`, `draft_initial`, `collapsed_globs`, `collapse_size_bytes`), §6.2 (first sight of a draft root), §6.4 (content model), §6.5 (watcher), §6.7 (TUI), and the scenarios `01-scenarios.md` D7, D8, F1–F3, plus the rulings frozen in this file. Read, in this order:

- `00-spec.md` §2, §3.5, §4, §6.1–6.7, §8 Phase 6, §10 from "2026-09-04 — Phase 5 close-out rulings" to the end, §11, Amendments.
- `01-scenarios.md` §D (D7, D8), §F.
- `docs/dev/engine.md` ("Scan pipeline in ten lines", "Many roots at once", "Watching"), `docs/dev/tui.md` ("Architecture", "Startup and the first frame", "The accept loop", "Hit-testing", "Adding a widget, with a snapshot", "The PTY harness", "Probes and logging"), `docs/dev/testing.md`, `docs/dev/bench.md`.
- `crates/lastcall-engine/src/{scan.rs, upstream.rs, watcher.rs, engine.rs, ops.rs, roots.rs, config/mod.rs}`; `crates/lastcall/src/tui/{run.rs, app.rs, render.rs, herdr.rs, term.rs}`; `crates/lastcall/src/commands/tui.rs`; `crates/lastcall-testkit/src/{fixture_parent.rs, fixture_repo.rs}`; the two test files named above.

## Entry baseline (inherited obligations)

- **Unit-test floor: 390** (`just test-unit` at Phase 5 close: lastcall lib 146, bin 5, engine 205, testkit 34; §10). The suite only grows; a shrinking count at any commit is a verifier failure class. Other tiers at close: e2e 29 snapshot scenes (59 `.snap` files) + 11 PTY scenes, real-herdr 4, prepush 64-case proptests, the integration golden.
- **§11 "`for-each-ref refs/remotes` twice per scan → Phase 6 agenda"** fires here: deliverable 5. The scan-spawn budget assertion (`engine.rs:1447`, `scan_spawns <= 18`, measured 17) must tighten, not merely hold.
- **§10 2026-09-04 (sponsor run):** four Rec rulings become deliverables 6–9 (draw coalescing and the no-op herdr draws; the `discovering…` frame; tracing; the nav offset). The undecided debounce cap is ruling item 2 here.
- **§10 2026-09-04 (Phase 5 close-out): "Claude Design pass after the Phase 5 merge, findings → Phase 6 kickoff agenda."** The pass has not run as of this draft. Ruling item 3 fixes what Phase 6 does about it; the three frames named there (`tui_accept_controls`, `tui_herdr_scope_notice`, `tui_herdr_scope_notice_with_status`) stay the pass's input either way.
- **§3.5 circle-backs:** none is scheduled for Phase 6. Q6 (750 ms debounce, hardcoded) is touched by ruling item 2 only as an additional bound, never as a config key.
- **§6.1 is not amended by this phase.** Every config key it needs already exists with the frozen default (`collapse_size_bytes` = 512 KiB, `collapsed_globs` = the lockfile list, `draft_initial` = `seen`).

## Entry preconditions (orchestrator-confirmed before launch)

1. Toolchain via rustup (`just toolchain`); cargo only through `just …` / `just cargo …`. The pre-commit hook runs fmt, clippy `-D warnings`, `check --no-default-features` and the unit tier (about a minute).
2. Your worktree branch was created from `main` and is locked to `.claude/worktrees/…`. **First action:** `git merge --ff-only feat/phase6-draft-dirs` so this kickoff is in your tree; if the tip has moved past your branch point, rebase your branch onto it before building.
3. Verified facts you may rely on (all at `839da62`):
   - **Draft roots end to end:** `roots::discover` makes a draft root of every absolute `draft_dirs` entry and of every relative glob match under each parent dir *and* each git root (`roots.rs:192–235`, §10 "Draft-glob semantics"); `Discovery::draft_dirs_inside` (`:62`) hands the enclosing git root the draft dirs to exclude from its `others`. First sight follows `draft_initial` (`engine.rs:1132`). `Fresh::restart()` in the F scenarios re-opens the engine over the same state dir; `pty_accept_loop_and_restart` does the same through the binary.
   - **Collapse decision:** blobs for every row are batch-fetched first (`scan.rs:502–523`); glob match (`:598–603`), then binary (a NUL in the first 8,000 bytes, `:270`), then size (either side **strictly** `> collapse_size_bytes`, `:641–645`, so 524,288 bytes is not collapsed and 524,289 is); collapsed rows keep `added`/`deleted` from `hunks::counts` — a full Myers pass (`hunks.rs:161`, `bench.md:100`) that every differing row pays — and skip only the second pass, `hunks::diff` (`:647–650`). A lockfile therefore costs one diff, never two; no git spawn depends on row content. D7 uses a one-line lockfile; D8 sets `collapse_size_bytes: 100`. Neither exercises the frozen defaults.
   - **Hunkless accept:** `a` and `A` on a row with no hunks are both "accept file" (`render.rs:353`, `app.rs:640`, unit test `app_accept_on_a_hunkless_row_is_still_the_whole_row`). Gate item 2's "single accept row" is already the behaviour; what is missing is the frame.
   - **Remote refs are listed twice per scan:** `Classifier::get` (`upstream.rs:77–90`) calls `remote_refs` at `:85` to build the key, then `classify` (`:154`, its only caller is `get`) calls it again for the same key. The `ClassifyKey.remotes` string is the only consumer.
   - **The TUI loop draws after every `Changed::Yes`** (`run.rs:707–709`), one `select!` arm per pass, no drain. The loop is one `async` block inside `run()` (`run.rs:556–716`) owning the real terminal, the signals, the link and the watcher; the test module (`run.rs:737+`) drives only the `Ui` wrapper (`tui.md`: "the unit-testable half of the loop"), so no test can queue events and count draws until deliverable 6 extracts the pass. `HerdrUpdate::Roots` returns `Changed::Yes` unconditionally (`app.rs:1312–1326`); `Herdr::apply_roots` (`herdr.rs:278–305`) removes each derived root from `self.roots` as it goes and drains the leftovers with `mem::take`, so "compare `self.roots` to `next` before the swap" is wrong — clone the map at entry. `Action::Press` resolves through the hit map of the last **drawn** frame (`run.rs:132–135`). `PaneAssociation` is emitted per pane on every resync (`client.rs:813, 910`) and is in `rederives`, so every 500 ms coalesce window ends in one no-op draw per pane.
   - **The screen opens after discovery:** `commands/tui.rs:36` runs `Engine::open` (about two seconds for 18 roots on the sponsor's machine) before `run::run` enters the terminal (`run.rs`, "Startup and the first frame" in `tui.md`), and `term::init_tracing` runs after the open (`:47`); the non-tty and bad-`[keys]` exits happen before that and must keep happening before the terminal is taken (`pty_non_tty_stdout_exits_2_without_drawing`, `pty_bad_keys_config_exits_2_before_taking_the_terminal`). Under raw mode Ctrl-C is a **key event**, not a signal (`tui.md`, the signal arm is for `kill -INT`), and every quit path exits **0** (`pty_ctrl_c_exits_cleanly`, `run.rs:532`); an `Err` from `run::run` is printed and mapped to exit **2** by `main.rs:86–91`. `engine.run(timings)` consumes the `Engine` by value (`run.rs:560`).
   - **Tracing:** `term::init_tracing` (`term.rs:104`) installs a file subscriber when `LASTCALL_LOG_FILE` is set, filtered by `LASTCALL_LOG` (default `info`); outside the herdr client the only calls are two `warn!` (`run.rs:423`, `:598`) and three toast `debug!` (`tui/herdr.rs:682–690`) — nothing in the engine or the watcher. `tracing-subscriber` (`env-filter`) is a dependency of the `lastcall` crate only; the engine crate has `tracing` and no subscriber. The watcher's scans run on `spawn_blocking` threads (`watcher.rs:356`), invisible to a thread-scoped test subscriber.
   - **Nav offset:** `render_nav` recomputes the window every frame from the selected line alone (`render.rs:598–603`); the window is over `lines` (root, branch, file, group and rule lines, `:565–600`), not over `nav_entries()`; the nav is not rendered at all below `NAV_MIN_COLS` = 70 (`:122`). `HitMap` (`render.rs:61`) is the only value that flows from a render back to the app (`Ui::rendered`, `run.rs:180`) and the snapshot helper never calls `rendered`; `App` has no scroll field (`app.rs:331–350`).
   - **Watcher scheduling:** `Scheduled::Scan(root)` sets `due[root] = now + debounce` on every event (`watcher.rs:566`), so the scan is trailing-edge with no upper bound (§6.5 says "per-path"; the code debounces per root — pre-existing wording drift). Three scan paths bypass `due`: the watcher-error path (`:549–551`) and the rescan backstop (`:596–599`) insert `now` directly, and `inspect_root` (`:396–425`) scans on a `HeadChange` without touching `due`. A root whose file never stops changing is scanned once per 30 s today (the backstop), because an unchanged HEAD scans nothing. `EngineTimings` (`:30`) carries `debounce`, `head_poll`, `rescan` and is built by `commands::poll_timings`.
   - **Testkit:** `fixture_parent::build` creates `W/alpha`, `W/beta` and the draft root `W/notes` (`DRAFT_DIR`) with first sight of all three and **asserts exactly three roots** (`fixture_parent.rs:82–88`); the status golden (`tests/golden/status_multi_repo.json`), every one of the 59 `.snap` files (`3 repos` in the header), every PTY scene (`scanning 3 roots…`) and `test_integration_herdr_worktree.rs:76` depend on that count — the fixture is not to be changed. `Scene::build` wraps it; `snapshot(name, &app, W, H)` at 100×30 writes `<name>_frame` and `<name>_styles` and never calls `rendered`; the PTY harness (`PtyTui`, `wait_first_piles`, `assert_clean_exit`, relaunch over one state dir as in `pty_accept_loop_and_restart` step 5) drives the release-shape binary. Every `Scene` frame already shows `notes` with the `draft` label beside a pending git root (`tui_nav_three_roots`). `e` is unbound in the default keymap; the help overlay lists every keymap action (`render.rs:1077–1088`).

## Deliverables

1. **Gate item 1 — a gitignored draft file, hunk-reviewed, accepted, surviving a restart (engine + TUI; first).**
   - (a) *Engine scenario:* extend F1 (`test_integration_scenarios_f.rs:17`) so the second edit produces two hunks, one is accepted with `accept_hunk`, the pile shows the remaining hunk, `restart()` shows the same remaining hunk, `accept_file` clears it, and a second `restart()` shows `[]`. This extended F1 **is** the gate's "scenario test"; (b) and (c) support it.
   - (b) *TUI scene:* a PTY scene `pty_draft_root_hunk_accept_and_restart`. **The scene, not the fixture, owns the fourth root:** after `fixture_parent::build` it creates `W/alpha/_drafts/reply.md`, appends `_drafts/` to `alpha/.gitignore`, writes its own `config.toml` with `draft_dirs = ["notes", "_drafts"]` (a relative entry matches under git roots too, `roots.rs:222–224`), and performs first sight **in-process** (`open_engine` + `scan_all`, four roots, `draft_initial = seen`) before the first launch — otherwise the binary's own first sight would see the edited file as the baseline. It asserts `scanning 4 roots…`; the row appears under `_drafts` with the `draft` label; `⏎` opens two hunks; `a` accepts the first; `q` exits 0; a relaunch over the same state dir shows the one remaining hunk; `A` clears the row. `fixture_parent::build` and its three-root assertion are untouched.
   - (c) *Snapshot:* the existing `tui_nav_three_roots` already shows a pending draft root with the `draft` label beside a git root — cite it; add `tui_draft_root_hunks`: the diff view of the draft root's two-hunk row (an `App` built over the scene's four-root engine), so the frame shows hunks under a `draft`-labelled root.
2. **Gate item 2 — lockfile churn is one accept row (engine + snapshot).**
   - (a) *Engine:* D7 grows to the scenario's shape: a committed `package-lock.json` of 4,000 lines rewritten so that `hunks::counts` reports thousands added and deleted; assert `Collapsed::Glob`, `hunks.is_empty()`, `added + deleted > 3,000`, and one `accept_file` clears it. No spawn-count claim (spawns do not depend on row content). If a cost number is wanted, a `test_bench.rs` scenario prints `scan_ms` for the 4,000-line lockfile under the default globs vs `collapsed_globs = []` — printed, never asserted.
   - (b) *Snapshot:* `tui_nav_collapsed_lockfile` at 100×30 — the nav row `package-lock.json ⊟  +2,113 −1,887`-style with the frozen default globs (no config override), and the diff view for it. The placeholder text changes per ruling item 1.
3. **Gate item 3 — binary and > 512 KiB render as collapsed rows (engine + snapshot).**
   - (a) *Engine:* D8 at the **frozen default** `collapse_size_bytes` (no override): a 2 MB file with a NUL in its first bytes → `Binary`; a 600 KiB text file → `Size`; the boundary: 524,288 bytes → not collapsed, 524,289 → `Size`. Keep the existing 100-byte variant as the fast case.
   - (b) *Snapshot:* `tui_nav_collapsed_binary_and_size` — both rows in the nav, the diff view on the binary row.
4. **Collapsed-row view (TUI + engine; ruling item 1).** Replace `expands in a later phase`. Per the Rec: a collapsed `Glob` or `Size` row opened with `⏎` shows `collapsed (<kind>) · +a −d · [e expand]` (the bracketed control is a click target, like `[a accept]`, so §6.7 mouse/keyboard parity holds — `input_parity_expand` test) and `e` (new keymap action `expand`) asks the engine on demand: `Effect::Expand(root, path)` → `watcher::blocking` (never inline on the UI task) → `Engine::hunks_of(root, &Row) -> Result<Expanded { hunks: Vec<Hunk>, omitted_lines: usize }, EngineError>`, computed from the row's own `baseline`/`current` oids via the store's batch cat and `hunks::diff` (so the expansion shows exactly the rendered delta, not whatever the store resolves later), truncated at `EXPAND_LINE_CAP = 2,000` hunk body lines (context included, separators excluded) → `Local::Expanded(root, path, Expanded)`. **The hunks live in `App.expanded: Option<(PathBuf, Vec<u8>, Expanded)>`, never in `row.hunks`:** `accept_scope` (`app.rs:643`) and `apply_pile` never read it, so `a` and `A` on a collapsed row stay whole-row (§6.3 "single accept") and a newer pile cannot collapse the view mid-read; `render_diff` (`render.rs:902–916`) consults it for the selected row; it is cleared when the selection leaves the row or a newer pile changes the row's oids; the footer reads `… N more lines`. `Binary` rows show `binary · <old> → <new> bytes` and `e` is a no-op there. Snapshots `tui_diff_view_collapsed_expanded` (new); `tui_diff_view_collapsed` and `tui_help_overlay` are regenerated (the placeholder text, the hint line, the `expand` row — sanctioned in Boundaries). Unit tests: the cap, the no-op on binary, the clear-on-newer-pile rule.
5. **One remote-ref listing per scan (engine).** `Classifier::get` passes the listing it already made into `classify` (signature `classify(rg, seen_head, state, user_email, remotes: String)`); the budget assertion at `engine.rs:1447` drops to `<= 17` with the comment's inventory updated ("one `for-each-ref refs/remotes`"); `engine_upstream_labels_follow_remote_refs_without_head_moving` (`engine.rs:2195`) keeps passing unchanged. `bench.md`'s spawn table rows S1/S2/S4 get the new counts from a release `just bench` run (their shapes differ from the unit budget's 17).
6. **Draw discipline (TUI).**
   - (a) *Coalescing:* extract `fn drain(ui: &mut Ui, sources: &mut Sources, link: &Herdr) -> Pass { changed: Changed, effects: Vec<Effect>, stop: Option<Stop> }`, where `Sources` holds `&mut mpsc::Receiver<EngineEvent>` (constructible with `mpsc::channel` in a test — the drain is written over the receiver, not over `Watcher`), the input receiver, the local receiver and `&mut Option<Receiver<HerdrEvent>>`. The `select!` arm folds its one event as today, then `drain` folds every `try_recv`-ready event up to `DRAIN_CAP = 256`, `or`-ing `Changed` and collecting effects in order. Rules: `Quit` and `Local::Fatal` end the drain immediately (`stop`); a mouse `Press` stops the drain (event left queued) when the pass already has `Changed::Yes`, so a press always resolves against a drawn frame (`run.rs:132–135`); wheel events fold freely. The loop runs the effects in order, breaks on `stop`, and draws **once** when `changed == Yes`. `Local::Roots` and piles in one drain are safe in either order (`orphan_piles` adoption, `app.rs:572–612`).
   - (b) *No-op herdr draws:* `HerdrUpdate::Roots` returns `Changed::Yes` only when `apply_roots` changed anything: `let before = self.roots.clone();` at entry, `delta.changed = before != next` after the episode carry-over (`RootFlag: PartialEq`) — the toast request keeps its current shape.
   - (c) *Tests:* in `run.rs`'s test module, `drain` over channels the test built: 17 queued piles → one `Pass { changed: Yes, .. }` and one `render_into`; a `Press` queued after a page-moving key stays queued; `Quit` stops mid-queue. In `herdr.rs`: a re-derivation with an unchanged map → `changed == false`; a status change → `true`. The existing PTY scenes unchanged.
7. **Something on screen during discovery (TUI; ruling item 4).** Per the Rec: `commands/tui.rs` prints `lastcall: discovering roots under <parent dir(s)>…` to **stderr** (stdout is the terminal-to-be) right before `Engine::open`, and moves `term::init_tracing()` ahead of the open so deliverable 8's `open done` line is loggable; the alternate screen then replaces the line. Nothing about the loop, the exit codes or the quit order changes. Test: a PTY scene asserting the line appears in the raw transcript **before** the alternate-screen-on sequence and the first frame after it; the two "exits 2 before taking the terminal" scenes unchanged. *If the sponsor takes the Alt instead*, the deliverable is the full restructure and must specify all of: `run::run` takes an `Open` closure; `Watcher` is `Option` until open completes and `Refresh`/`Accept`/`SyncRoots`/`Focus`/rescan effects are dropped while `None`; the input channel is served during discovery with only `Quit` acting (Ctrl-C is a key under raw mode) and exit stays **0**; the open-result arm runs the initial `Resize`, `sync_roots`, the `scanning N roots…` status and `herdr_rederive` (the link may connect before open finishes) and returns `Changed::Yes`; an open failure calls `restore()` then returns `Ok(ExitCode::from(1))` (an `Err` would exit 2); `RealShutdown.watcher` accepts `None` and a Ctrl-C mid-open abandons the blocking thread (first-sight ledgers are atomic-rename, a half-built store is re-initialised on the next open — asserted by relaunch); `run` becomes generic over the ratatui backend so an order test can run on `TestBackend`; plus snapshot `tui_discovering_frame`, the order test, and `pty_open_error_restores_the_terminal_then_exits_1` (unwritable `LASTCALL_STATE_DIR`; `lastcall:` appears after the alternate-screen-off sequence).
8. **Tracing where the diagnosis needed it (engine + TUI).** `tracing::debug!` lines with stable field names, emitted where a thread-scoped test subscriber can see them: **engine** (`Engine::scan`, `Engine::open`) — `scan done root= ms= rows= seq=`, `open done roots= ms=`; **watcher** — `watch event path= kind= scheduled=`, `scan due root= reason=(event|head|rescan|refresh)`, `head inspect root= changed=`, `rescan backstop`, `watch installed roots=`; **TUI** — `draw cause= ms=` and `fold source= changed=`. `docs/dev/tui.md` "Probes and logging" documents the grammar and the one-liner (`LASTCALL_LOG=debug LASTCALL_LOG_FILE=/tmp/lc.log lastcall tui`). Tests: an engine unit test wrapping a direct `engine.scan(&root)` in `tracing::subscriber::with_default` over a small `Layer` collecting `String`s, asserting `scan done` with `rows=`; `tracing-subscriber` added as an **engine dev-dependency** (workspace version); no test asserts timings; never `set_global_default` in a test.
9. **Persistent nav scroll offset (TUI).** `App.nav_top: usize` over nav **lines** (the `lines` vector `render_nav` windows, not `nav_entries()`); `render_nav` starts from it, moves it only when the selected line is outside `[top, top + rows)` (above → `top = selected`; below → `top = selected + 1 − rows`), clamps it to `lines.len().saturating_sub(rows)`, and reports the value it used in `HitMap.nav_top: Option<usize>` — `None` when the nav was not rendered (below `NAV_MIN_COLS`), so `Ui::rendered` writes back only `Some` and a hidden nav does not reset the offset; `sync_roots`/`apply_pile` leave it alone (the clamp handles a shrinking list). The first jump past a page still lands on the bottom row and the snapshot helper never calls `rendered`, so every existing snapshot holds unchanged. Tests (render.rs and app.rs, phrased over line indices): selected line 25 of 40 in 20 rows → top 6; then line 20 → top stays 6; a click on a visible row → top unchanged and the clicked row selected; line 5 → top 5; the list shrinking under the offset clamps; nav hidden at 60 columns then shown at 100 keeps the offset.
10. **Debounce starvation cap (engine; ruling item 2).** Per the Rec: `EngineTimings.debounce_max: Duration` (default 3 s, hardcoded like the debounce, not a config key): the first event for a root records `first_seen[root]`; each later event sets `due[root] = min(now + debounce, first_seen + debounce_max)`; `first_seen` is cleared **wherever the root is scanned** — the `ready` drain, the rescan and watcher-error paths, and `inspect_root`'s `HeadChange` scan — through one `scanned(root)` hook paired with the extracted `schedule(due, first_seen, root, now, timings)`. Unit tests over `schedule`/`scanned`: a 300 ms cadence for 6 s produces a scan at ≈3 s and another ≈750 ms after the end; a head-change scan mid-burst does not leave a redundant capped scan behind. Steady state, stated not hidden: a root whose file never stops changing (a log file outside `ignore_globs`) is scanned once per 3 s for as long as the writer runs instead of once per 30 s today; an unchanged pile is `Changed::No` so nothing redraws. `--poll` unchanged. Two amendment proposals ratified by the merge: **B12 — sustained edit burst** in `01-scenarios.md` (both expectations above), and **Amendment v1.6** to §6.5/§6.1: "debounced per root on the trailing edge (750 ms of quiet) with a 3 s cap from the first event of a burst, both hardcoded (Q6)" — which also fixes the pre-existing "per-path" wording; `engine.md` "Watching" updated with it.
11. **Docs.** `docs/dev/engine.md`: a "Draft roots and collapsed classes (Phase 6)" section (first sight, the store without alternates, the collapse ladder, `hunks_of`, the debounce cap); `docs/dev/tui.md`: the draw pass, the discovering frame, `nav_top`, the collapsed view keys, the logging grammar; `README.md` config table rows for `draft_dirs`, `draft_initial`, `collapsed_globs`, `collapse_size_bytes` if absent; `00-spec.md` §8 Phase 6 gate lines and §10/§11 entries are the orchestrator's at the docs commit, not the worker's.

**Worker split (ruling item 5):** one Opus 5 worker, deliverables in the order above (1–5 engine-heavy first, then 6–10, docs last), on the phase branch `feat/phase6-draft-dirs`. Report with evidence at the end of deliverable 5 (a sanctioned checkpoint: the orchestrator runs the tiers and the first verifier before the TUI half starts).

## Boundaries

- Never touch `~/.config/herdr/`, `~/.config/lastcall/`, `~/.local/state/lastcall/`, `z_ignore/`, or the reference clones `~/dev/git/herdr` and `~/dev/git/drydock`. Every test runs through an injected `Env` over a temp dir.
- Never `git push`, never commit on `main`, never `git add -A`/`git add .`, never `rm -rf`. `cargo` only through `just`.
- Frozen contracts (§5, §6, `01-scenarios.md`) change only by an amendment proposal in your report; the two expected proposals are B12 and Amendment v1.6 (deliverable 10).
- Snapshot updates (`just snapshots-update`) only for the new scenes and for the two sanctioned regenerations — `tui_diff_view_collapsed` (placeholder text and hint line) and `tui_help_overlay` (the `expand` row); any other existing `.snap` changing is a finding to report, not a file to regenerate.
- `just test-prepush` before your final report; the unit count in the report, by crate.

## Gate evidence forms (what the PR table will cite)

| Gate item | Evidence |
|---|---|
| Draft file pending → hunk-reviewed → accepted → survives restart (scenario test) | **F1 extended** is the scenario test; supporting: `pty_draft_root_hunk_accept_and_restart` (PTY), `tui_draft_root_hunks` (snapshot), `tui_nav_three_roots` (existing) |
| Lockfile churn = one accept row (snapshot) | `tui_nav_collapsed_lockfile` at the frozen default globs; supporting: D7 at 4,000 lines (`hunks.is_empty()`, counts > 3,000) |
| Binary and > 512 KiB collapsed (snapshot) | `tui_nav_collapsed_binary_and_size` at the frozen default; supporting: D8 at the 524,288/524,289 boundary |
| Agenda: one `for-each-ref` per scan | budget assertion at 17; `bench.md` table |
| Agenda: draw coalescing, no-op draws | the two `run.rs` loop tests; a `LASTCALL_LOG=debug` capture from the sponsor's layout showing one `draw` per batch (orchestrator-run) |
| Agenda: discovering line/frame | Rec: the PTY scene (line before alternate-screen-on); Alt: `tui_discovering_frame`, the order test, the open-error scene |
| Agenda: tracing | the capturing-subscriber test; `tui.md` grammar |
| Agenda: nav offset | the five offset tests; sponsor re-check of the "click on a visible row" case |
| Ruling 2: debounce cap | the `schedule`/`scanned` unit tests; B12 + Amendment v1.6 |

## Rulings (all Rec — sponsor, 2026-09-05; the Alts stay on record)

1. **Collapsed-row expansion.** Rec: on-demand expansion for `Glob` and `Size` rows via `e`, computed lazily with a 2,000-line cap, whole-row accept only (deliverable 4) — D7's "no hunk view *by default*" reads as "there is one on request", and the placeholder text has been promising it since Phase 3. Alt: keep the placeholder, reworded to `collapsed (<kind>) · +a −d` with no expansion, and let Phase 8 (editing) decide — right if the sponsor never wants to read a lockfile diff in lastcall; it still regenerates `tui_diff_view_collapsed` (the wording) but nothing else.
2. **Debounce starvation cap.** Rec: a 3 s cap (deliverable 10, with Amendment v1.6 to §6.5 — the review found that a scan without 750 ms of quiet is a contract change, not an implementation detail) — the sponsor's core complaint on the Gate 5 run was "nothing until the very end"; a writer that never pauses 750 ms is exactly the case that reproduces it for real, and a scan every 3 s costs ~16 git spawns per root per 3 s at worst, including for a root with a never-quiet log file (30 s today). Alt: trailing-edge only, as today — right if one coalesced pile at the end of a burst is what a reviewer wants to read and partial piles during a burst would be noise; the diagnosis showed the agent's actual pattern (one command per batch) never triggers the starvation.
3. **Claude Design pass timing.** Rec: Phase 6 does not wait for the pass; it moves to the **Phase 9 kickoff** agenda (before release hardening), the three named frames stay its input, and Phases 6–8 add no new layout concept without a note in `docs/dev/tui.md` naming it as pass input — Phase 6 touches the nav and the first seconds but not the status line, header ladder or scope notice. Alt: the sponsor runs the pass now, before the worker launches, and its findings become Phase 6 deliverables — right if the pass is a week or less away and its findings would change the nav work in deliverables 7 and 9.
4. **Discovering frame scope.** Rec (changed by the review): keep `Engine::open` before the terminal and print one stderr line `lastcall: discovering roots under <dir>…` that the alternate screen replaces — a five-line change that answers "is it hung?" for the discovery seconds, with zero loop risk on a phase that already reshapes the loop (deliverable 6). Alt: take the terminal first and draw a real `discovering…` frame — the review costed it (an `Option<Watcher>` loop, Ctrl-C as a key with exit 0, a new open-failure exit path, re-derive on open, a backend-generic `run` for the order test, three new tests); right if the sponsor wants Ctrl-C to work *during* discovery or expects root counts where discovery takes ten seconds or more.
5. **Worker split.** Rec: one Opus 5 worker with a sanctioned checkpoint after deliverable 5 — the engine half is small and the TUI half depends on `hunks_of` and the loop shape. Alt: two workers (6a engine 1–5, 10; 6b TUI 4, 6–9) on one branch like Phase 5 — right if wall-clock matters more than the merge cost.
