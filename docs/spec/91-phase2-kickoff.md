# Phase 2 Kickoff Prompt (operational artifact, not design)

Phase 2 of the lastcall phased program. Phase 1 closed on 2026-09-01 (PR #1, merged `692e43a`): the workspace, `just` targets, CI on both OSes, the config layer (`lastcall_engine::config`, every §6.1 key, injected `Env`), the herdr client behind the `herdr` feature, the mock server, and the deterministic fixture-repo builder (`lastcall_testkit::fixture_repo::FixtureRepo`) all exist. Nothing reviews anything yet. This phase builds the **review engine**: the private object store, the seen-tree ledger, the private-index scan, pending computation with hunks, the accept operations, git awareness (HEAD tracking, upstream annotation, notices), root discovery, draft roots, the watcher, crash safety, and a headless `lastcall status --json`. It closes Gate 2 (§8, Phase 2). Phase 3 puts a TUI on top of exactly the types you build here, so the library surface is the product.

**The spine of this phase is the seen-tree model (§6.2, §10 "Ledger baseline model") and invariants 1, 2, 3 and 5 (§2).** The baseline for every path is *content*: a git tree in a private store we own, plus per-path overrides. `pending(p) = diff(baseline(p), worktree(p))`, computed fresh on every scan and never stored (invariant 1: the ledger is recomputable from scratch; a restart costs a rescan). **HEAD is never consulted for correctness** — not for baselines, not for candidates, not for first sight after the first sight. An agent's commit changes no worktree bytes, so nothing leaves the pile (invariant 5, scenario B1); a branch switch over-shows and self-clears (B4); a content-preserving rebase re-flags nothing (B5). Git history is an **annotation layer** (§6.4): it can add a label, a group, or a notice, never remove a row. Every resolution failure — missing object, unparsable override, unreadable ledger, index lock held — falls to the next step and **over-shows** (invariant 2): the tool may show too much after a failure; it must never hide a change. Every accept is **compare-and-swap** against the rendered content hash, never a fresh read of disk (invariant 3, F3 in the spec's review history): an agent write between render and click yields a refusal, never a silently blessed change. If you find yourself reaching for `HEAD`, `git status`, or a re-read of the live file to decide what is pending or what to accept, stop — that is the bug the model was chosen to eliminate.

**The executable reference implementation is the Phase 0 shell harness** `scripts/harness/lc.sh` (151 lines) with `scenarios.sh` (46/46 green, `just harness`). It is the git plumbing of §6.1–6.5 as shell functions, and it surfaced four rules that are now in the frozen spec. Port it function by function; when your Rust disagrees with it, the harness is right until a test proves otherwise, and then the *spec* is what you cite in your report.

---

## Mission

Build Phase 2 per [`00-spec.md`](00-spec.md) §8 "Phase 2 — Headless engine" (deliverable text and gate checklist are authoritative), against the frozen contracts §6.1 (storage layout, identity and durability rules), §6.2 (ledger schema v1.0, baseline resolution, first sight, overrides, accept-all/compaction), §6.3 (operations; restore, flag export and editor save are later phases), §6.4 (git awareness — every rule there is a testable claim), §6.5 (watcher, HEAD watching, private index, root discovery), the platform contract §4, the test strategy §7.1–7.2 and §7.4, and the invariants §2. The test plan is [`01-scenarios.md`](01-scenarios.md): every **H** and **E** scenario in sections A–F becomes a named integration test (the exact list is in Gate 2). Reference implementations to study before writing code:

- `scripts/harness/lc.sh` — every function, in this order: `lc_init` (store, alternates, config-key copy), `lcg` (the store-plumbing environment), `lc_first_sight`, `lc_seed_index`, `lc_write_tree_of_disk`, `lc_hash` (symlink branch, cwd-relative form), `lc_mode`, `lc_baseline` (resolution order), `lc_candidates` (refresh + `diff-files` + `ls-files --others` + override paths, skip-worktree filter), `lc_pending_p` (content OR mode, fail-open on a missing baseline), `lc_upstream_paths` (the classifier, verbatim §6.4), `lc_pile`, `lc_accept_file`, `lc_accept_all` (index-info + write-tree), `lc_snapshot_rendered` (the CAS snapshot), `lc_restart`.
- `scripts/harness/scenarios.sh` — each `assert_pile` line is an integration-test assertion you port 1:1 (deliverable 12 gives you the same output format in Rust so the expected strings copy over unchanged).
- `crates/lastcall-testkit/src/fixture_repo.rs` — `FixtureRepo` (seed files, `commit_files`, `coworker_push`, fixed identity and dates). Extend it; do not fork it.
- `crates/lastcall-engine/src/env.rs` and `config/mod.rs` — the injected `Env` and the config types you consume (`Loaded`, `Resolved { parent_dirs, notices }`, `state_dir`, `draft_dirs`, `draft_initial`, `collapsed_globs`, `collapse_size_bytes`, `ignore_globs`).
- `crates/lastcall-engine/src/herdr/client.rs` — the house style for an injected-timings state machine with an mpsc output (`ClientTimings`, `HerdrEvent`). The engine's watcher loop follows the same shape.

## Entry baseline (inherited obligations)

- **Unit-test floor: 101** (`just test-unit` at Phase 1 close: engine 85, testkit 16, bin 0; §10). The suite only grows; a shrinking count at any commit is a verifier failure class.
- Phase 1 follow-ups **F6** (hello-herdr double ping) and **F8** (hello-herdr needs a resolvable state dir) are logged, not yours; do not touch `hello_herdr.rs` beyond what compiling requires.
- No amended gate items from Phase 1.

## Entry preconditions (orchestrator-confirmed before launch)

1. Toolchain 1.98.0 via rustup (`just toolchain` prints `cargo 1.98.0`). Run cargo only through `just …` / `just cargo …` (agent shells may not see rustup on a bare `cargo`). Git is 2.37.1 locally (Apple), newer on CI runners; every plumbing flag you use must exist in **2.31+** (`--path-format=absolute` is 2.31). The engine checks `git --version` once at open and refuses to start below 2.31 with a notice.
2. Your worktree branch was created from `main` and is locked to `.claude/worktrees/…`. Your **first action**: `git merge --ff-only feat/phase2-engine` (allowlisted) so this kickoff and the Gate 1 docs commit are in your tree; verify with `git log --oneline -3` showing "docs: Phase 2 kickoff" at the top.
3. Verified-on-real-git facts (2026-09-01, git 2.37.1) you may rely on without re-proving: `git hash-object -- <symlink>` **follows the link** and hashes the target (link blobs come from `readlink` + `hash-object --stdin`); `git ls-files --others` from a foreign `GIT_DIR` never lists `.git` and lists a nested repository as a single `dir/` entry; `git ls-files -v` tags skip-worktree entries `S `; with cwd at the root, `hash-object -- <rel>`, `--stdin-paths`, and `--stdin --path=<rel>` all apply `text=auto` identically — **the harness trap is the cwd, not the flag**: from outside the work tree normalization is skipped; with zero remotes `rev-list <range> --not --remotes` lists every commit (so nothing classifies as upstream, which is correct).

## Deliverables

1. **`git.rs` — the single spawn site.** Exactly one module spawns `git`; a gate grep proves it (`rg -n 'Command::new\("git"\)' crates/lastcall-engine/src` → one file). Two runners, both taking the injected `Env`:
   - `StoreGit` — every command against **our** store: `Command::new("git").current_dir(<root>)` (cwd **is** the root — the `text=auto` rule, §6.4) with `GIT_DIR=<store>`, `GIT_WORK_TREE=<root>`, `GIT_INDEX_FILE=<our index or a temp index>` set explicitly on every call, plus `-c core.excludesfile=<the user's effective core.excludesfile, if any>`.
   - `RepoGit` — read-only inspection of the **user's** repo: `git -C <root>` with `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` **removed** from the child env. Its command set is a closed allowlist enforced by a unit test over the runner's own API: `rev-parse`, `symbolic-ref`, `config --get`, `ls-files` (`-v`, `-u`, `--stage` forms only), `rev-list`, `merge-base`, `log --format`, `diff-tree`, `cat-file`, `for-each-ref`, `worktree list --porcelain`, `--version`. **Never** `status`, `diff`, `add`, `update-index`, `checkout`, `stash`, `gc`, or anything that can take the user's `index.lock` or write their repo — the user's index is theirs (skip-worktree bits are *read* from it, D6).
   - Both: `GIT_OPTIONAL_LOCKS=0`, `GIT_TERMINAL_PROMPT=0`, `LC_ALL=C`, inherit the process env otherwise (so the user's real global config is honored in production), and overlay every `GIT_*` / `XDG_*` / `HOME` variable present in the injected `Env` (so tests set `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_SYSTEM=/dev/null`, `GIT_CONFIG_NOSYSTEM=1` through `Env` and the engine's children see them). `-z` on every path-producing command, with NUL-parsers for `diff-files -z` (`:<omode> <nmode> <ooid> <noid> <status>\0<path>\0`), `ls-files -z`, `ls-files --stage -z`, `ls-tree -z`, `diff-tree -z`, `cat-file --batch-check`. Paths are bytes (`Vec<u8>` / `OsStr`) end to end; JSON output renders them lossily and a non-UTF-8 path is reported pending with a notice and refuses accept in v1 (over-show; record it as an informational deferral in your report). Errors carry the argv, cwd, exit status and stderr (`GitError`, thiserror).

2. **`paths.rs` + `store.rs` — identity and the private object store (§6.1).** `RootId`/`ParentId` = the first 16 hex chars of SHA-256 (`sha2` crate) of the **canonicalized** absolute path (`fs::canonicalize`; on macOS `/tmp` → `/private/tmp`), recorded in `meta.json` / `ledger.json`; on open a recorded path that no longer canonicalizes to the same string is a different root (E4: first sight, plus a best-effort notice naming any sibling `repos/*/ledger.json` under the same parent whose recorded root no longer exists on disk). Layout exactly as §6.1: `<state>/roots/<parent-hash>/meta.json`, `repos/<repo-hash>/{ledger.json, store/, index, lock}`. The store is `git init -q --bare`; for git roots write `store/objects/info/alternates` = `git -C <root> rev-parse --path-format=absolute --git-path objects` (for a linked worktree this resolves to the common object dir — that is the D10 rule) and copy `core.autocrlf`, `core.eol`, `core.filemode`, `core.ignorecase` from the user's effective config into the store's config **at every open**, not only at init (the user may change them). Draft roots: same store, no alternates, no key copy. Operations: `hash_path(rel) -> Oid` (`hash-object -w -- <rel>` from cwd=root; symlinks via `lstat` → `readlink` → `hash-object -w --stdin`, mode `120000`), `hash_bytes(bytes)` (`hash-object -w --stdin`, no `--path`: the bytes are already-normalized baseline content, used by accept-hunk), `cat_blob(oid) -> Vec<u8>`, `exists(oid)` via `cat-file --batch-check` (batchable), `ls_tree(tree) -> BTreeMap<path, (mode, oid)>` (`ls-tree -r -z`), `write_tree(entries)` (`update-index --index-info` into a **temp** index seeded by `read-tree <seen_tree>` or `read-tree --empty`, then `write-tree`; removals are `0 0000…0\t<path>`; the temp index is unlinked after), `tree_of_disk()` for draft first sight (a temp index, `update-index --add -z --stdin` over the file list; never `git add`). **Never run `gc`, `prune`, `repack -d`, or `fsck --lost-found` in the store**: nothing in it is anchored by a ref, so pruning would delete the seen tree and every override blob. The store grows by one loose object per scanned version of each changed file; that is accepted for v1 and goes in your report as an informational deferral (hardening shape: `refs/lastcall/seen` + an overrides tree ref, then `gc` becomes safe).

3. **`ledger.rs` — schema v1.0 (§6.2 verbatim), fail-open loading, atomic writes, locking.** Serde types matching the JSON in §6.2 field for field (`schema_version: "1.0"`, `root`, `kind`, `seen_tree`, `seen_at { head_commit, branch, at }`, `overrides { <path>: { blob?, mode?, flag?, updated_at } }`), `deny_unknown_fields` **off** (a newer minor version must still load; unknown fields are dropped on rewrite — say so in a comment). Loading rules, each a unit test: missing file → first sight; unparsable file or unknown `schema_version` major → the file is **moved aside** to `ledger.json.unreadable-<unix-secs>` (never overwritten, never deleted), the root opens as first sight, one notice; an override with an unparsable shape → that path resolves to the tree (E2), one notice, the rest of the ledger intact. Saving: serialize to `ledger.json.tmp` in the same directory, `sync_all`, `rename` (§6.1 write ordering: objects first, then the ledger — accept-hunk's blob is written before the ledger references it). Every read-modify-write of the ledger holds `<repo>/lock` via `std::fs::File::try_lock` (stable since 1.89; no `nix` in product crates) with a bounded retry, re-reads the file under the lock, applies, writes, renames — so `lastcall status` and a future TUI on the same root cannot lose each other's writes. `seen_at.at` and `updated_at` come from an injected `Clock` trait (production: `SystemTime`; tests: fixed), so ledger round-trip tests are byte-stable. **Baseline resolution** is one function, `baseline(path) -> Baseline { Present{oid, mode} | Absent | Empty }` implementing §6.2's order (override blob → override `null` = Absent → tree entry → Empty) and verifying `exists(oid)` before trusting any override or tree oid — a missing object steps to the next rule and records a notice (E2/E3). Unit-test the order with a fake store.

4. **`index.rs` — the private index, a cache and never truth (§6.5).** `seed(seen_tree)` = `read-tree <tree>` (or `--empty`) into `<repo>/index`, recording the tree it was seeded from in `<repo>/index.tree`; on open, if `index.tree` ≠ `ledger.seen_tree` or the index is missing/unreadable, reseed. `refresh()` = `update-index -q --refresh --ignore-submodules` (exit status ignored: it is non-zero when files differ); if git reports `index.lock` held (another lastcall process), retry three times with 50 ms backoff, then **scan without refresh** — `diff-files` still lists every candidate, at worst over-reporting, so the scan stays correct. `diff_files() -> Vec<(path, status, old_mode, old_oid)>` (`diff-files -z --ignore-submodules=all`). `others()` = `ls-files --others -z` with `--exclude-standard` for git roots plus `--exclude-from=<user git dir>/info/exclude` when that file exists (the store's own `info/exclude` is empty; `--exclude-standard` would otherwise miss the user's), no excludes for draft roots. Entries ending in `/` are nested repositories (D9) — never candidates of this root; they are reported to root discovery. A test deletes `index` and `index.tree` between two scans and asserts identical piles (the index is a cache).

5. **`scan.rs` — candidates → pending rows.** Port `lc_candidates` + `lc_pending_p` + `lc_pile`: candidates = `diff_files` paths ∪ `others` ∪ every override path, minus paths tagged `S` in the **user's** index (`RepoGit ls-files -v -z`, D6), minus paths under a nested repo or a draft root owned by another root. For each candidate: `current = lstat` → `Absent` | `{oid: hash_path, mode}` where mode is `120000` for symlinks, `100755` when executable **and** the store's `core.filemode` is not `false`, else `100644`; a directory where a file was (or vice versa) is a type change. `baseline` from deliverable 3. A row is pending iff `baseline != current` by oid **or** by mode (D1: a mode-only change is a row with one synthetic hunk `mode 100644 → 100755`). Row fields: `path`, `change: modified|added|deleted|mode|typechange`, `baseline: Option<{oid, mode}>`, `current: Option<{oid, mode}>`, `added`/`deleted` line counts, `hunks` (deliverable 6), `annotation` (deliverable 9), `conflicted` (path listed by the user's `ls-files -u`), `collapsed: Option<glob|binary|size>` (D7/D8: `collapsed_globs` match, git's binary heuristic — a NUL in the first 8000 bytes of either side — or either side larger than `collapse_size_bytes`; collapsed rows carry counts but no hunks), `flag` (from the override), `rename: Option<{ from | to, similarity }>` (D5: only when the pile has both deletions and additions — a temp index seeded from the seen tree, `git add -N -- <added paths>` under `StoreGit`, then `diff -M -z --name-status`; pairing is presentation, the ledger stores delete + add). Rows sorted by path bytes. **Every scan re-hashes only candidates**, never the whole tree; the stat cache makes the common case a stat-only pass (a debug counter of `hash-object` calls per scan, asserted in one test: a second scan with no changes issues zero hashes).

6. **`hunks.rs` — the diff model.** Line diff of two byte buffers (the `similar` crate, `TextDiff::from_lines` over `&[u8]`, unified hunks at **context 3** so hunk boundaries match `git diff`'s), never requiring UTF-8. `Hunk { index, old_range, new_range, lines: Vec<(tag, bytes)> }`; `counts(a, b) -> (added, deleted)` must equal `git diff --numstat <a> <b>` — assert it in the D3 test (CRLF, `text=auto`: 1/1, not 3/3, because both blobs come from the store, already normalized) and on three more rows. `apply_hunk(baseline, hunks, i) -> Vec<u8>` splices exactly hunk `i`'s change lines into the baseline. **Property test** (proptest, `cases: 1000`, in the unit tier, seed and count printed in the report): for random `(baseline, current)` line sequences with random edits, for every hunk `i`: let `b' = apply_hunk(baseline, hunks, i)`; then the multiset of tagged change lines in `diff(b', current)` equals the original multiset minus hunk `i`'s change lines (context lines excluded from the comparison, since neighbours may regroup) — "never resurrects the accepted hunk, never drops an unaccepted one"; and applying every hunk in any order yields `current` exactly. Commit any `proptest-regressions/` files that appear.

7. **`ops.rs` — accept operations, all CAS (§6.3), plus compaction.** Inputs are **rendered tokens** taken from a scan's rows, never fresh reads: `Rendered { path, oid: Option<Oid>, mode: Option<Mode> }`.
   - `accept_file(rendered)`: re-hash the live path with `hash_path` (`-w`, so the store holds exactly what was compared); if `(oid, mode)` ≠ rendered → `Refused::Moved { live }` and no ledger write (A6). Else override `{blob: oid, mode}`; if that equals the seen-tree entry, remove the override instead — keeping any `flag` (§6.2 clean-up rule). `seen_head` is untouched.
   - `accept_deletion(rendered with oid None)`: refused if `lstat` succeeds; else override `{blob: null}` (A7).
   - `accept_hunk(rendered, hunk_index)`: CAS as accept_file; `b' = apply_hunk(baseline_bytes, hunks, i)`; `oid' = hash_bytes(b')` (written first), then override `{blob: oid', mode: rendered.mode}` (A3). Baseline `Absent`/`Empty` → the baseline bytes are empty.
   - `accept_all(snapshot: &Pile)`: one mechanism for accept-all **and** compaction — `fold(snapshot)`: temp index = `read-tree <seen_tree>` (or empty) ⊕ every override (`blob` → entry with the override's mode; `null` → removal) ⊕ every row of the snapshot (rendered `oid`/`mode`; `Absent` → removal); `write-tree`; then `seen_tree = new`, every override loses its `blob`/`mode` (a flag-only override stays; an override with neither is deleted), and for **accept-all only** `seen_at = { head_commit: HEAD, branch, at: now }`; reseed the index. A5: a row whose disk content moved after the snapshot is folded at its *rendered* oid, so the next scan shows only the post-confirm delta. Existing overrides on paths that are not pending are folded in too — otherwise clearing them would re-flag content the user already accepted (this is why the harness's `lc_accept_all` iterates override paths as candidates).
   - `compact()` = `fold(&Pile::empty())` with `seen_at` **unchanged** (§6.2: "invisible to the user and changes no baseline"; moving `seen_head` would un-group still-pending upstream rows). Triggered automatically after any ledger write when overrides carrying a `blob` exceed `EngineOptions::compaction_threshold` (default 500, injectable; E5 tests with 10). Pile before == pile after, asserted.
   - `flag(path, note)` / `unflag(path)`: set/clear `flag` on the override without touching `blob` (A8's Phase 2 slice: schema + retention through accept-all; export is Phase 7). Restore is **Phase 7**: do not implement it.
   - **Fault injection for E1:** every op takes a `&dyn FaultInjector` with `fn at(&self, point: FaultPoint)`; production passes `NoFault`; points are `AfterObjectWrite` and `AfterLedgerTmpWrite`. The testkit's implementation SIGKILLs the current process (`nix::sys::signal::kill(getpid(), SIGKILL)` — testkit may use `nix`; the engine may not). This is the one test seam in product code; name it in a comment.

8. **`headstate.rs` — HEAD, branch, in-progress state, notices (§6.4 "HEAD tracking", "In-progress operations").** `inspect(root) -> HeadState { head: Option<Oid>, branch: Option<String>, detached, in_progress: Option<Merge|Rebase|CherryPick|Revert>, merge_head: Option<Oid>, shallow }` from `rev-parse HEAD`, `symbolic-ref -q --short HEAD`, the presence of `MERGE_HEAD`/`CHERRY_PICK_HEAD`/`REVERT_HEAD`/`rebase-merge/`/`rebase-apply/` under `rev-parse --git-dir` (the per-worktree dir, D10), `rev-parse --is-shallow-repository`. `transition(prev, next, last_reflog_line) -> Option<Notice>`: the last line of `<git-dir>/logs/HEAD` is a **hint for the text only**; classification by message prefix: `commit`/`commit (amend)`/`commit (merge)` → "committed on <branch> (N commits)" (N = `rev-list --count prev..next`); `checkout: moving from A to B` → "switched A → B (same commit)" or "switched A → B: N files differ from seen state" (N from the scan that follows); `rebase (finish)`/`rebase -i (finish)` → "rebased <branch> onto <onto>"; `reset: moving to X` → "reset: moving to X"; `pull:`/`merge` → "merged …"; unknown → "HEAD moved: <message>". Notices are suppressed while `in_progress` is set and emitted once when it clears. A wrong notice text is a cosmetic bug; a notice that changes any baseline or row is a spec violation. Unit-test the classifier on literal reflog lines; integration-test B1–B3, B7 against real reflogs.

9. **`upstream.rs` — the annotation classifier, harness-verified (§6.4 "Upstream annotation").** Port `lc_upstream_paths` + the per-path rule in `lc_pile` exactly: heads = `HEAD` + `MERGE_HEAD` when present; per head, range = `seen_head..head` if `merge-base --is-ancestor seen_head head`, else `merge-base(seen_head, head)..head`, and no merge-base → no annotation (C7); `seen_head` is `ledger.seen_at.head_commit`, `None` → no annotation. Per commit: upstream iff remote-reachable **and** neither author nor committer email equals the user's `user.email` (`RepoGit config --get user.email`; unset → the identity test passes vacuously, which is the §11 heuristic); local merge (≥2 parents, not upstream) → its `diff-tree --cc` paths go to **M**; other local commits → **L**; upstream → **U**. Per pending row: in U∖(L∪M) and `current.oid` equals the path's blob at some head (`rev-parse -q --verify <head>:<path>`) → `upstream`; in U and (in L∪M, or differs from every head) → `mixed`; else none. Batch what the tests keep green: `rev-list <range> --not --remotes` once per head gives the not-remote set; `log --format='%H%x00%ae%x00%ce%x00%P' <range>` gives identities and parent counts; `diff-tree --no-commit-id -r --name-only -z [--cc] <c>` per commit. Detached HEAD with no upstream, shallow, or zero remotes → no annotation, individual rows. Group rows are derived: `groups = [{kind: upstream, paths}]` for the rows tagged `upstream` (§6.7 renders them as "upstream · N files"; the rows stay in `pending` too — the JSON is lossless). Tests: C1–C8, B5, B9 (an agent's own pushed commit stays an individual row — the identity test is what makes that true).

10. **`roots.rs` — discovery and rescan (§6.5 last paragraph, §6.4 "Nested git"/"Worktrees").** For each resolved parent dir P: if P is inside a git repo (`rev-parse --show-toplevel` succeeds from P) that repo is a root; otherwise every immediate child directory of P with a `.git` entry (directory **or** file — a linked worktree's `.git` is a file) is a git root. Draft roots: each `draft_dirs` entry that is absolute names a directory; a relative entry is a glob matched against directory paths relative to each parent dir **and** relative to each discovered git root (so `_drafts/**` matches `<parent>/_drafts` and `<repo>/_drafts`; the matched directory is the root; a trailing `/**` is accepted and ignored). A draft root inside a git root is owned by the draft root: the git root's scan excludes that subtree (F1's `_drafts/` is gitignored in the scenario, but the rule must not depend on that). Nested repositories reported by `others()` (D9) become git roots with `badge: nested_in`. Linked worktrees: `rev-parse --git-dir` ≠ `--git-common-dir` → `badge: worktree_of <main worktree>` (from `worktree list --porcelain`'s first entry), HEAD watching under the per-worktree git dir (deliverable 11). Parent association for state layout: a root discovered under configured parent P uses P; an ad-hoc cwd root uses its own parent directory. `rescan()` runs at startup and on `EngineTimings::rescan` (default 30 s, injectable) and emits `RootsChanged { added, removed }`; roots keep their ledgers when removed from the watch set (never delete state).

11. **`watcher.rs` — `notify` + HEAD watching + polls (§6.5), invariant 9.** One `notify::RecommendedWatcher`; every root watched recursively, events filtered by `ignore_globs` (`globset`) — and **`ignore_globs` scopes the watcher only**: the scan enumerates from the private index regardless, so an edit under `vendor/` is discovered on the next scan (test: with `vendor/**` ignored, an edit to a tracked `vendor/x` appears on a manual rescan and after the next poll). Per root, the git-dir allowlist watched explicitly (§6.5 verbatim): `HEAD`, `index`, `ORIG_HEAD`, `MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`, `packed-refs`, `refs/` (recursive), `rebase-merge/`, `rebase-apply/`, under `--git-dir` **and** `--git-common-dir` when they differ; FSEvents is recursive by nature, so filter events to that allowlist rather than trusting the watch granularity. Events only ever **schedule**: a root scan (trailing-edge debounce, `EngineTimings::debounce` = 750 ms, hardcoded in production per G0 Q6, injectable in tests) or a HEAD inspection; a HEAD poll every `head_poll` (10 s) and the rescan timer are the invariant-9 backstops; a watcher error/overflow schedules a full scan of every root. Output is `tokio::sync::mpsc<EngineEvent>`: `Pile { root, pile }` after every scan, `Head { root, from, to, branch, notice }`, `RootsChanged`, `Notice { root: Option, text }`. Subprocess work runs on `spawn_blocking` (or a dedicated thread), never on a runtime worker. Gate item 4 ("a commit with no other file activity triggers re-inspection within the poll interval") is a real-time integration test with `head_poll = 250 ms`, deadline 5 s, asserting a `Head` event with `notice` starting "committed on main".

12. **`engine.rs` + `status.rs` + the CLI.** `Engine::open(loaded: &Loaded, resolved: &Resolved, env: &Env, options: EngineOptions) -> Engine` (git version check, state dir, discovery, ledgers, first sight per §6.2 — git root `seen_tree = HEAD^{tree}` and `seen_at.head_commit = HEAD`, no commits → `null`; draft root per `draft_initial`); `engine.roots()`, `engine.scan(root_id) -> Pile`, `engine.scan_all()`, `engine.ops(root_id)` (deliverable 7), `engine.run(timings) -> (JoinHandle, Receiver<EngineEvent>)` (deliverable 11). `StatusReport` (`status_version: 1`) serializes to the JSON below; **no timestamps, no hostnames, roots sorted by path, rows by path bytes**, so a golden file is possible:
    ```json
    { "status_version": 1,
      "notices": ["watching /x ad hoc: not under any parent_dirs in /y/config.toml"],
      "roots": [ { "root": "/abs/canonical", "kind": "git|draft", "parent": "/abs/parent",
        "badge": null | {"worktree_of": "/abs/main"} | {"nested_in": "/abs/outer"},
        "head": "<sha>|null", "branch": "main|null", "in_progress": null|"merge"|"rebase"|"cherry-pick"|"revert",
        "seen_tree": "<sha>|null", "seen_head": "<sha>|null",
        "pending": [ { "path": "f1", "change": "modified", "baseline": {"oid": "…", "mode": "100644"}|null,
          "current": {"oid": "…", "mode": "100644"}|null, "added": 1, "deleted": 1, "hunks": 1,
          "annotation": null|"upstream"|"mixed", "conflicted": false, "collapsed": null|"glob"|"binary"|"size",
          "flag": null|{"note": "…"}, "rename": null|{"from": "…", "similarity": 90}|{"to": "…", "similarity": 90} } ],
        "groups": [ {"kind": "upstream", "paths": ["u1", "u2"]} ],
        "notices": ["…"] } ] }
    ```
    CLI: `lastcall status [--json] [--root <path>…]` (one-shot: open, scan all, print, exit 0; exit 1 with the error when the state dir is unwritable) and `lastcall watch [--json] [--exit-after <secs>]` (runs `engine.run`, prints one line per `EngineEvent` — the notices are the visible behaviour for B1/B2/B7; the JSON form prints one `EngineEvent` per line). Human `status` format: `<root name> (<branch>)  N pending` then `  M f1  +3 −1  [upstream]` rows and `  upstream · N files` group lines. The testkit gains `pile_lines(&Pile) -> Vec<String>` producing **exactly the harness's format** (`"path"`, `"path upstream"`, `"path mixed"`, sorted) and an `assert_pile!(engine, root, "f2 mixed|u1 upstream")` macro, so every `assert_pile` in `scenarios.sh` ports with its expected string unchanged.

13. **Testkit extensions and the golden.** `FixtureRepo`: set `user.name`/`user.email` in the repo's **local** config at `new()` (the classifier reads config, not env), add `write(path, bytes)`, `remove(path)`, `symlink(link, target)`, `chmod_x(path)`, `checkout_b(name)`, `checkout(name)`, and `engine_env(state_dir) -> Env` (a minimal `Env` with `HOME` = a temp dir, `LASTCALL_STATE_DIR`, and the three git-config isolation variables) — tests never construct `Env::from_process()`. Golden: `crates/lastcall/tests/test_integration_status_golden.rs` builds one temp parent dir holding (a) a repo with an uncommitted edit plus a later agent commit (B1), (b) a repo after a fast-forward pull of two coworker files (C2: upstream group) with one of them edited on top (C6: mixed), (c) a draft dir with one edited file (F3), runs the **built binary** (`env!("CARGO_BIN_EXE_lastcall")`) with `LASTCALL_CONFIG`/`LASTCALL_STATE_DIR` pointing into the temp dir, replaces the temp prefix with `<W>` and compares byte-for-byte against `crates/lastcall/tests/golden/status_multi_repo.json`; `LASTCALL_UPDATE_GOLDEN=1` rewrites it (`just golden-update`). Deterministic by construction: oids depend only on content and the fixture's fixed dates.

14. **`just` targets, docs, hygiene.** New targets: `just probe-status` (release build; builds the golden's three-root fixture via a testkit example `crates/lastcall-testkit/examples/fixture_parent.rs`; runs `target/release/lastcall status` and `status --json`; prints both), `just probe-watch` (release build; `watch --exit-after 8` against the same fixture while the example commits in repo (a) at t+2 s and edits at t+4 s; prints the event lines — the B1 notice must appear), `just golden-update`, `just test-scenarios` (= `cargo test -p lastcall-engine --test 'test_integration_scenarios*'`). Add `"Bash(git merge --ff-only:*)"` is already in the allowlist for you; add nothing else. Docs: `docs/dev/engine.md` — the storage walkthrough with real commands (`ls ~/.local/state/lastcall/roots/*/repos/*`, `jq . ledger.json`, `GIT_DIR=store git ls-tree -r <seen_tree>`, `GIT_DIR=store git cat-file -p <override blob>`), the two git runners and the read-only allowlist table, the `status --json` schema above, the scan pipeline in ten lines, and the fail-open ladder; update `AGENTS.md` (golden path gains `just probe-status`, a stub for `docs/dev/engine.md`), `README.md` (a "Try it" section: `lastcall status` in any repo), `docs/dev/testing.md` (scenario tests, the golden, how to update it, the new floor number). `docs/spec/00-spec.md`, `01-scenarios.md`, and this file are read-only for you.

## Traps (each is a verified failure; design them out)

- **`hash-object` must run with cwd = root and a root-relative path.** From any other cwd, `text=auto` is not applied and a CRLF file diffs as whole-file churn (harness D3: 3/3 instead of 1/1). `StoreGit` sets `current_dir(root)` unconditionally.
- **`hash-object -- <symlink>` follows the link** (verified). `lstat` first; link text via `--stdin`.
- **`ls-files --others` lists nested repos as `dir/`**, never their contents; it never lists `.git`. Treat a trailing `/` as "another root".
- **The user's `info/exclude` is invisible to our store's `--exclude-standard`**; pass `--exclude-from`. Their global `core.excludesfile` is honored automatically in production (we inherit their config) and nulled in tests (`GIT_CONFIG_GLOBAL=/dev/null` through `Env`).
- **Skip-worktree lives in the user's index**, not ours (D6). Without the `S` filter every sparse-excluded file is a false pending deletion.
- **`MERGE_HEAD` is a classification head and `--cc` paths are mixed** (C4/C5). A merge in progress has no merge commit yet; the classifier must not wait for one.
- **Never touch the user's index.** `git status`, `git diff` (porcelain) and `git add` on their repo can take `index.lock` and rewrite it; `RepoGit`'s allowlist test is the guard. `GIT_OPTIONAL_LOCKS=0` everywhere as belt and braces.
- **`GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` leak both ways.** Set all three explicitly for store commands; remove all three for user-repo commands; never rely on inheritance (the sponsor may run lastcall from inside a git hook or an agent shell that exports them).
- **Our index is a cache.** Reseed whenever `index.tree` ≠ `seen_tree`; a deleted index must produce an identical pile.
- **`index.lock` contention** between two lastcall processes: retry, then scan unrefreshed (over-show), never fail the scan.
- **Nothing in the store is ref-anchored.** No `gc`/`prune`/`repack -d` on it, ever. Plumbing commands never trigger auto-gc; `git commit`/`merge` would, and we never run them.
- **`write-tree` needs every blob present.** Draft first sight must write the blobs (temp index via `update-index --add`), not just list paths.
- **Compaction must not move `seen_head`**; accept-all does.
- **Accept-all folds existing overrides, not only pending rows** — otherwise clearing overrides re-flags accepted content.
- **`similar` hunks at context 3** so the TUI's hunks match `git diff`; the property test compares change lines, not context.
- **`FixtureRepo` uses env identity for its own commits; the classifier reads `user.email` from config.** Set the local config key in `new()` or B9/C1–C8 misclassify.
- **`notify` on macOS coalesces and may deliver a parent-directory event**; never derive "which file changed" from an event — it only schedules a scan of the root (invariant 9).
- **libtest swallows output of passing tests**: skips (D4 on a case-sensitive filesystem; anything else you must skip) are written with `stderr().write_all` and named in the report.
- **`git add -A` / `git add .` are denied** in your shell; stage explicitly. `git push` never. Worktree and `scripts/harness/work/` and `z_ignore/` are gitignored and not yours.
- **First sight writes state from a "read-only" command.** `lastcall status` on a never-seen root creates the ledger, by design (§6.2). Say so in `engine.md`.

## Gate 2 (closes this phase; evidence forms are what will be accepted)

- [ ] **Scenario suite, every scenario a named integration test** in `crates/lastcall-engine/tests/test_integration_scenarios_{a,b,c,d,e,f}.rs`, one test per scenario ID named `scenario_<id>_<slug>`, each asserting with `assert_pile!` strings copied from `scenarios.sh` where the harness covers it: **A1–A8** (A3 with the store; A6 refused accept; A8 = flag set, retained through accept-all), **B1–B9** (B1 asserts the notice text; B5 the rebase re-flags nothing; B9 the pushed agent commit stays individual), **C1–C8** (C4 both during the conflict and after resolution; C7 fallback and no-merge-base; C8 documented heuristic), **D1–D6** (D4 macOS-only, skipped with a reason on a case-sensitive FS), **D7–D11**, **E1–E5**, **F1–F3**. Paste the `cargo test` summary lines and the full list of test names. Every scenario marked (H) must reproduce its harness expectation exactly.
- [ ] **Property test** `hunks_accept_then_recompute_never_resurrects_or_drops` green over ≥1000 cases (config `cases: 1000`; paste the seed line and count) plus `hunks_apply_all_in_any_order_yields_current`.
- [ ] **Crash recovery** `scenario_e1_kill9_between_object_write_and_ledger_rename_recovers`: parent test computes the pre-accept pile (oracle), spawns a child role (re-exec of the test binary with an env marker) that runs `accept_hunk` with the SIGKILL injector at `AfterObjectWrite`; asserts the child died by signal 9, the ledger bytes are identical to before, the orphan object exists (`cat-file -e`), and a reopened engine's pile equals the oracle. A second case at `AfterLedgerTmpWrite` asserts the same and that `ledger.json.tmp` is cleaned up on reopen.
- [ ] **HEAD-movement detection** `watcher_commit_without_file_activity_triggers_head_inspection` with `head_poll = 250 ms`: a commit of already-staged content (no worktree write) yields a `Head` event with notice "committed on main (1 commit)" within 5 s; plus `watcher_ignore_globs_do_not_hide_tracked_edits`.
- [ ] **Golden** `status_json_multi_repo_matches_golden` green from the built binary; the golden file committed; `just probe-status` output pasted.
- [ ] **Unit floor:** `just test-unit` ≥ 101 at every commit and the final count per crate pasted (this number is the Phase 3 floor).
- [ ] **Greps, pasted:** `rg -n 'Command::new\("git"\)' crates/lastcall-engine/src` → exactly one file; `rg -n 'std::env::var|home_dir\(' crates/lastcall-engine/src` → only `env.rs`; `rg -n deny_unknown_fields crates/lastcall-engine/src/herdr` → none; `rg -n deny_unknown_fields crates/lastcall-engine/src/config` → ≥1; `rg -n '"status"|"add"|"checkout"|"stash"' crates/lastcall-engine/src/git.rs` → none as a `RepoGit` argv (the allowlist unit test is the real guard; the grep is the quick check); `rg -n '\bHEAD\b' crates/lastcall-engine/src/{scan,ledger,ops,index,store}.rs` → none (HEAD is consulted only in `headstate.rs`, `upstream.rs`, and first sight in `engine.rs`); `rg -n '^libc' crates/*/Cargo.toml` → none; `rg -n 'gc|prune|repack' crates/lastcall-engine/src/store.rs` → only the comment forbidding them.
- [ ] **Built-artifact pass:** `cargo build --release`, `./target/release/lastcall status` and `status --json` from inside the lastcall checkout itself (ad-hoc cwd notice, the repo as a root, its own pending rows), `just probe-status`, `just probe-watch` (the B1 notice line visible) — outputs pasted.
- [ ] **Standing:** `just lint` clean; no test weakened or deleted; unit tier deterministic (no git subprocesses in unit tests except against temp fixtures, no sleeps > 50 ms, proptest seeded by its own persistence); `cargo check -p lastcall-engine --no-default-features` green (the engine never depends on the herdr client); `git diff main -- docs/spec/00-spec.md docs/spec/01-scenarios.md docs/spec/91-phase2-kickoff.md` empty; `cargo tree -e normal -p lastcall -p lastcall-engine | grep -c testkit || true` → 0; new dependencies limited to `similar`, `globset`, `sha2` (product) and `proptest` (dev), each MIT/Apache, exact versions in `Cargo.lock`.

## Operational rules

- **Sacred and untouchable:** the sponsor's real state dir (`~/.local/state/lastcall`), config (`~/.config/lastcall`, `~/.config/herdr`), every repo under `~/dev/git/` other than your worktree (including the read-only reference clones; the built-artifact pass runs `status` against **your worktree checkout** with `LASTCALL_STATE_DIR` set to a temp dir — never the real state dir), and `z_ignore/`. Every test injects `Env`; `engine_env` is the only way tests obtain one. A test that canonicalizes, watches, or hashes anything outside a temp dir is a verifier failure.
- **Frozen artifacts (STOP, human-only):** `00-spec.md` §5 and §6, `01-scenarios.md`, this kickoff. A contract change you need is implemented additive and no-op-when-absent and reported as a *proposed v1.2*. Specifically: the `status_version: 1` JSON is a new surface (not frozen; Phase 3 consumes it), `index.tree` and `lock` are additive files under the §6.1 layout (report them as a proposed §6.1 editorial addition), and the `FaultInjector` seam is product code that must be a no-op in production.
- **Spend:** no LLM calls in the product. This is the largest construction phase: expected worker spend ≈ $1–2; flag in your report if you project > $3; hard tripwire $5 — stop, commit clean, report. Estimate in the commit message of each milestone commit.
- **Commit discipline:** conventional commits, first line < 72 chars, phase-shaped in this order: git runners → store+paths → ledger → index → scan+hunks → ops (+ property test) → headstate → upstream → roots → watcher → engine+status+CLI → scenario tests by section (may interleave with the module they exercise) → golden+probes → docs. Each commit green under `just lint && just test-unit` (`just hooks-install` first). Resumable from git alone: if your context is compacted, `git log` and this file must be enough to continue. NO `git push`. NO `git add -A`. Verifier fixes are separate `fix(review):` commits.
- **Never end your turn to wait for anything.** No notification will come. Poll bounded cycles in-turn (watcher deadlines, child processes). There is no sanctioned mid-phase checkpoint; your next turn-end after starting is the final report.

## Working agreements

- Grounded claims only: test names and counts, file paths, SHAs, pasted output. "Works" is not evidence.
- Fresh-context **verifier subagents** (Agent tool, adversarial charter) at two milestones; findings and resolutions go in the report:
  - **(a) after deliverables 1–7 with sections A, D, E, F green.** Hunts: an accept path that reads the live file instead of comparing to the rendered oid (construct the A6 race by hand); a baseline or candidate computation that consults HEAD; `hash-object` invoked from a cwd other than the root or on a symlink path; a hidden row — build adversarial worktrees (file→symlink, file→directory, deletion + recreate with the seen content after an accepted deletion, mode flip with `core.filemode=false`, a non-UTF-8 path, a path with a newline) and prove each shows or is explicitly reported; a ledger write that is not tmp+fsync+rename; the index treated as truth (delete it → different pile?); a `RepoGit` invocation outside the read-only allowlist; compaction moving `seen_head`; accept-all dropping a non-pending override; a test with zero assertions; unit tests that spawn git outside a temp dir.
  - **(b) before the final report.** Hunts: an annotation rule that removes a row (grep for filters after classification); the classifier ignoring `MERGE_HEAD` or grouping a same-identity commit (B9); a notice path that writes the ledger; watcher events applied as state (any cache written from an event); `ignore_globs` reaching the scan; nondeterminism in the golden (run it twice, run it from a different cwd, run with `TZ`/`LANG` changed); a scenario test whose expected string differs from `scenarios.sh` for an (H) scenario; suite count shrinking across commits; frozen-doc edits; the greps in Gate 2; `probe-status` against the real state dir.
- Stop conditions: gate met; spend tripwire; a frozen-artifact edit becomes tempting; a genuine human-only blocker. Commit clean, document, stop.

## Final report

Gate 2 self-assessment, one row per gate item with the evidence pasted. Then: unit-test count per crate and total (the Phase 3 floor), the full scenario-test name list mapped to scenario IDs, the property-test seed/count line, verifier findings with resolutions, built-artifact outputs, the exact `just`/`cargo`/`git` versions, proposed spec additions (v1.2 candidates: `index.tree`, `lock`, `status_version`, draft-glob semantics, non-UTF-8 path handling, store growth), and open questions split into **needs-a-ruling** (design choices the spec left open — name the choice and the alternative) and **informational**. Total spend accounting.
