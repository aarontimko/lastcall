# Phase 5 Kickoff Prompt (operational artifact, not design)

**Rulings: PENDING.** The adversarial design review has not run yet; the sponsor rules on the items under "Rulings" after it, and this header is rewritten to FROZEN with the verdicts. Until then no worker exists.

Phase 5 of the lastcall phased program. Phase 4 closed on 2026-09-03 (PR #4, merged `b716f49`): the seen-state write path exists — `Engine::accept` (hunk, file, root, all) with compare-and-swap refusals, the engine-global scan sequence, the 10,000-row cap, the accept keys `a` / `shift-a` / `ctrl-a`, thousands separators everywhere, the fast unit tier (8 proptest cases) and the pre-push tier (integration + 64 cases), and the four-scenario bench with its S1–S4 baseline in `docs/dev/bench.md`. The herdr client (`crates/lastcall-engine/src/herdr/`, feature `herdr`) has existed since Phase 1 with discovery, the protocol guard, the two-tier connection design of §5.4/§6.6, resync coalescing, orphan reconciliation and dedupe — all tested against the in-crate mock server. Nothing in the TUI consumes it yet: `commands/tui.rs` opens the engine and calls `run::run`; `hello_herdr.rs` is the only consumer.

**The spine of this phase is §6.6's first sentence: a dedicated task owns every socket and translates herdr state into messages; nothing in the review engine depends on it.** herdr is an annotation layer over the ledger, exactly as git is (§6.4). Every screen this phase adds must render identically with herdr absent, mismatched or mid-reconnect, minus the annotations. The second spine is the entry obligation: the first frame must be fast before a single herdr pixel is drawn, because the sponsor's real workload (50 clones, thousands of rows) is what the flag-in-the-nav demo runs on.

**The reference implementations:** `crates/lastcall/src/commands/hello_herdr.rs` (discovery → `probe` → `Client::spawn` → event loop — the exact call shapes the TUI adopts), `crates/lastcall-engine/src/herdr/client.rs` (its `#[cfg(test)]` module is the mock-server test style: `MockHerdrBuilder`, `status_events`, `drop_event`, `close_lifecycle_after`), `crates/lastcall-engine/tests/test_integration_herdr_real.rs` (the one test that runs against the pinned real binary — its bootstrap and done-derivation drive are the shape of the new real-server tests), `crates/lastcall/tests/test_e2e_tui_snapshots.rs` (pure-reducer scenes) and `test_e2e_tui_pty.rs` (`PtyTui`), `crates/lastcall/tests/test_bench.rs` + `docs/dev/bench.md` (the instrument this phase must move).

---

## Mission

Build Phase 5 per [`00-spec.md`](00-spec.md) §8 "Phase 5 — herdr in the UI" (deliverable text and the eight gate items are authoritative, including the entry obligation added at the Phase 4 close-out), against the frozen contracts §5 (the herdr surface, still verified at release tag **v0.8.2**, protocol 20 — see "Entry preconditions" 3), §6.6 (client behavior), §6.7 (UI contract), §5.7 (status model, local ack), §5.9 (`notification.show` rules), §7.5 (compat workflow), and the scenarios G1–G8 in [`01-scenarios.md`](01-scenarios.md). Read in this order, in full:

- `00-spec.md` §2, §3.5, §4.4, §4.6, §5 (all), §6.4–6.7, §7, §8 Phase 5, §10 from "2026-09-02 — Phase 4 close-out rulings" to the end, §11, Amendments.
- `01-scenarios.md` §G.
- `docs/dev/engine.md` ("The two git runners", "Scan pipeline in ten lines", "Watching"), `docs/dev/tui.md` (Architecture), `docs/dev/testing.md`, `docs/dev/bench.md`.
- `crates/lastcall-engine/src/herdr/{mod.rs, client.rs, wire.rs, discovery.rs, guard.rs}` — the public types you consume: `Client::spawn(transport, ClientTimings, ClientOptions { reconnect }) -> (ClientHandle, Receiver<HerdrEvent>)`, `ClientHandle::{snapshot() -> Option<Cache>, shutdown(), is_finished()}`, `HerdrEvent::{Connected, Disconnected, Standalone, Lifecycle, Resync, AgentStatusChanged, PaneAssociation, WorktreeChanged, StatusStreamOpened, StatusStreamClosed}`, `Cache { version, protocol, focused_*, workspaces, panes, agents, resyncs }`, `PaneInfo` (`cwd`, `foreground_cwd`, `agent`, `agent_status`, `workspace_id`, `is_agent_bearing()`), `WorkspaceInfo` (`agent_status` rollup, `worktree: Option<WorkspaceWorktreeInfo { repo_key, repo_name, repo_root, checkout_path, is_linked_worktree }>`), the wire method constants and their params/result types.
- `crates/lastcall-engine/src/{engine.rs, store.rs, headstate.rs, git.rs, watcher.rs, ops.rs, env.rs, config/mod.rs}` and `crates/lastcall/src/tui/{app.rs, render.rs, input.rs, run.rs}`, `crates/lastcall/src/commands/{tui.rs, hello_herdr.rs}`, `crates/lastcall-testkit/src/{mock_herdr.rs, pty_tui.rs}`, `justfile`, `.github/workflows/ci.yml`.

## Entry baseline (inherited obligations)

- **Unit-test floor: 318** (`just test-unit` at Phase 4 close: lastcall lib 106, bin 4, engine 185, testkit 23; §10). The suite only grows; a shrinking count at any commit is a verifier failure class.
- **Entry obligation — open/scan cost (§8 Phase 5, first gate item; §11 "Engine open and scan are serial and git-spawn bound").** Baseline, bench S1 (100 roots / 4,000 rows, release build, the machine block in `bench.md`): `open_ms` 33,1xx with `open_spawns` 2,902 (29 per root), `scan_all_ms` 25,6xx with 1,900 spawns (19 per root), `first_pile_ms` 33,6xx, `first_frame_ms` 56,568 (open plus one scan of every root, as `bench.md` defines it), ≈13 ms per spawn. Targets: `first_frame_ms` ≤ 6,000 at **50 roots / 4,000 rows** and S1 (100 roots) improved ≥ 4× on `first_frame_ms`. Built first, as deliverable 1, before any herdr UI work.
- **§11 "One `index.tmp` per root is shared by every lastcall process"** — trigger "Phase 5 kickoff" fires: ruled here (item 2) and built as deliverable 2. Workspace scoping (deliverable 8) makes several lastcall processes over one state dir the *normal* case, not an accident.
- **§11 "Title-change `pane_updated` events drive a snapshot per command"** — hardening scheduled for Phase 5: deliverable 9.
- **§11 "v0.8.2 lifecycle-subscription replay on connect"** stays open: the released herdr is still v0.8.2 (verified 2026-09-03, `gh release list`), so every connect replays up to 512 stale events. Deliverable 4 must tolerate it (the client already coalesces; the TUI must not flash stale flags — see Traps).
- **§3.5 circle-back "after Phase 4"** (ack semantics, accept-all confirmation threshold): evaluated at this kickoff, item 7 under "Rulings" — no change proposed.
- **§3.5 circle-back "after Phase 5"** (§5 re-verification against the release tag): performed at this kickoff against v0.8.2 via `gh api repos/herdrdev/herdr/contents/<path>?ref=v0.8.2` (the reference clone carries no tag object): no drift found in the surfaces §5 names; new facts recorded under "Entry preconditions" 3. The worker repeats the check for the surfaces it adds (`agent.focus`, `workspace.get`) and pastes the result in the report; the PR records both.
- **Amendment A3 (ruling 1 of 2026-09-03, sponsor verbatim in §10): hunk accepts drop the live CAS and keep the baseline CAS.** Built as deliverable 3; the §6.3 wording change is proposed as v1.5 at the docs commit and ratified by the sponsor merging the PR.
- **Claude Design pass:** the sponsor plans a design pass over the TUI (§10, Phase 4 close-out). Item 8 under "Rulings" fixes when it happens and what it receives; this phase's screens are drawn to be replaced, not defended.

## Entry preconditions (orchestrator-confirmed before launch)

1. Toolchain 1.98.0 via rustup (`just toolchain`); cargo only through `just …` / `just cargo …`. `std::fs::File::{lock, try_lock, unlock}` are stable on this toolchain — deliverable 2 needs no new crate.
2. Your worktree branch was created from `main` and is locked to `.claude/worktrees/…`. **First action:** `git merge --ff-only feat/phase5-herdr-ui` so this kickoff is in your tree; if the tip has moved past your branch point, rebase your branch onto it before the merge. Then `just test-unit` and confirm 318 before writing a line.
3. Verified facts you may rely on (release tag v0.8.2 unless noted):
   - **`agent.focus`** takes `AgentTarget { target: String }` (`src/api/schema/common.rs:44`); the server resolves `target` first as a public pane id (`parse_current_public_pane_id` → must be an agent-bearing pane), then as an agent name with a single-match rule (`src/app/terminal_targets.rs:75–105`). The public pane id is the `pane_id` string in `session.snapshot` — pass it verbatim. Errors to handle by name: `agent_not_found`, `agent_target_ambiguous` (an unknown-method error is a version signal, §4.6).
   - **`workspace.get`** takes `WorkspaceTarget { workspace_id }` and `workspace.list` takes `EmptyParams` (`src/api/schema.rs:68–72`); both return the `WorkspaceInfo` shape we already type, including `worktree` provenance. `session.snapshot` already carries every workspace, so deliverable 8 needs no new call in the common case.
   - **Pane environment:** herdr exports `HERDR_WORKSPACE_ID`, `HERDR_TAB_ID`, `HERDR_PANE_ID` only to panes launched with `PaneLaunchIdentity::Managed` (`src/pane.rs:136–151`); `Inherit` panes get none, `OmitPane` removes the pane id. Absence is therefore a normal case, not an error; `HERDR_SOCKET_PATH` (`src/api/mod.rs:20`) and `HERDR_ENV=1` are separate. All of it reaches you through the injected `Env` (`env.var("HERDR_WORKSPACE_ID")`), never `std::env`.
   - **`herdr api schema --json`** prints the schema file embedded at build (`src/cli/api.rs:1,25–31`, `docs/next/api/herdr-api.schema.json`); `--summary`-style text output and `--out <path>` exist too. That is the input of deliverable 11.
   - **Toasts:** §5.9 holds (1/sec global rate limit; `delivery = off` default; herdr's own toast at the `done` instant). The lifetime of herdr's own toast is in `src/app/api.rs` (the `duration` match on `toast.kind`, master line 741) — read it at the tag and set `TOAST_DELAY` above it (deliverable 6).
   - **Existing mock-server tests already cover most of the sixth gate item** (all in `client.rs`'s test module): `client_parses_both_envelope_shapes_on_one_stream`, `client_subscribe_failure_closes_connection`, `client_pane_not_found_at_construction_does_not_retry`, `client_tears_down_orphan_status_subscription_at_resync`, `client_coalesces_tab_focused_into_one_snapshot`, `client_coalesces_pane_focused_into_one_pane_get`, `client_dedupes_repeated_status_events`, `client_converges_after_dropped_pane_closed_via_fallback_resync`, `client_reconnects_after_stream_close_with_full_bootstrap`, `client_snapshot_resync_emits_done_to_idle_flip`, `client_worktree_events_are_relayed_and_schedule_a_resync`, plus `wire_lifecycle_burst_fixture_parses_both_envelope_shapes`. The gate item is met by *naming* them in the report and adding what this phase introduces (title-only `pane_updated`, toast scheduling, ack state); do not rewrite them.
   - **Engine hot spots:** `Engine::open` (`engine.rs:253`) → `rescan` (`:358`; the two serial loops at `:361` and `:404`) → `open_root` (`:417`); `scan_all` (`:660`) iterates roots serially, up to three passes for nested repos; `scan` (`:598`) takes `&mut self` for `scan_seq` and the root state. Per-root spawns at open: `Store::open` (`store.rs:105–200`: `config --get core.excludesfile`, the neutralised-config writes, `git_path objects`, four `COPIED_CONFIG_KEYS` × get + set/unset, `git_path info/attributes`, `config --get core.filemode`), then `git_path info/exclude`, `headstate::inspect` (`symbolic-ref`, `rev-parse --is-shallow-repository`, two `rev-parse --path-format=absolute --git-*`), `config --get user.email`, `config --get remote.origin.url`, the index seed. `git::spawn_count` is the counter the bench reads.
   - **TUI loop:** `run::run` builds a multi-thread runtime, `engine.run(timings)` → `Watcher { events, engine: Arc<Mutex<Engine>>, .. }`, one `tokio::select!` over signals / `watcher.events` / input / a local channel (`Local::{Pile, Accepted, Roots, RefreshDone, Notice, Fatal}`); the reducer `App` is pure and `render` is a function of `App`. `RootMeta { path, name, kind, parent, badge, branch, head, in_progress, remote }` is what the nav draws for a root.
   - `MockHerdrBuilder` / `MockHerdr` / `MockControl` (`lastcall-testkit/src/mock_herdr.rs`) serve a scripted session over a real socket (`canned(method, result)`, `status_events`, `lifecycle_events`, `drop_event`, `close_*`, `delay`, `stall`; `control().requests()` records every request) — the PTY herdr scenes and the G-scenario unit tests use it; `just mock-herdr` serves one by hand.

## Deliverables

1. **Entry obligation — open/scan cost (engine; worker 5a; first).**
   - (a) **Bounded parallel root open.** `rescan` opens newly discovered roots on a scoped thread pool (`std::thread::scope`, bound `EngineOptions.parallelism: usize`, default `min(available_parallelism(), 8)`, floor 1); results are inserted in path order so `Engine` state is byte-identical to the serial result (the `status --json` golden proves it). Per-root failures stay per-root (an `Err` root does not fail the others, as today). Hold no engine lock across the pool: `open_root(&self, ..)` already borrows immutably.
   - (b) **Bounded parallel scan.** `scan_all` runs the per-root scan on the same pool. Split `scan` into a pure per-root worker (`&RootState` + store → pile and state delta, no `scan_seq`) and a serial apply step that assigns `scan_seq` in path order and stores the results; `scan(&mut self, root)` for one root keeps its signature and behaviour. The nested-repo passes stay serial between passes. `EngineEvent::Pile { seq }` ordering guarantees (§10 Phase 4, "engine-global") are unchanged.
   - (c) **Per-root call list collapsed.** Replace the per-key `config --get`s of open (`core.excludesfile`, the four copied keys, `core.filemode`, `user.email`, `remote.origin.url`) with one `config --list -z` read, parsed once per root; batch `headstate::inspect`'s four calls into one `rev-parse --path-format=absolute --git-dir --git-common-dir --is-shallow-repository --abbrev-ref HEAD` (verify the output order and the detached-HEAD `HEAD` line; keep `symbolic-ref` only if `--abbrev-ref` loses information you need) and fold `git_path objects|info/attributes|info/exclude` into `--git-path …` flags of the same call; skip the private store's `config set/unset` when the value it already holds matches (one `config --list -z` of the store, or a fingerprint file beside it). The `RepoGit` allowlist widens by exactly these two shapes (`config --list -z`; multi-flag `rev-parse` with `--git-path`), documented in `engine.md` "The two git runners" (ruling item 6). Expect ≤ 10 spawns per root at open and ≤ 12 per scan; report the counts, they are not the gate.
   - (d) **Bench.** `just bench` S1 re-run before and after on the same machine, plus a new scenario **S1h** = S1 at **50 roots / 4,000 rows** (the sponsor's stated workload, the gate's absolute target). `bench.md` gains an "after" table per scenario (before columns kept), the S1h table, and one paragraph on what changed. Print `BENCH S1h …` lines like the others.
   - (e) **Tests.** Unit: a fixed-pool determinism test (parallelism 1 vs 4 over a temp parent of ≥ 6 fixture roots → identical `status --json`); a test that one unopenable root under parallel open leaves the others open; the `config --list -z` parser (empty values, `=`-containing values, `\n` inside values); the batched `rev-parse` parser on detached HEAD and on a linked worktree. The existing scenario suites and the golden are the regression net.
2. **N-instance hardening (engine; worker 5a; ruling item 2 — rec: graceful N).** (a) The temp index becomes per-process: `index.<pid>.tmp` under the root's store, created lazily, removed on `Engine` drop, and stale `index.*.tmp` whose pid is not alive are removed at `Store::open`. (b) Every accept's read-modify-write of a root's ledger runs under an advisory lock on `<store>/ledger.lock` (`File::lock`, held only inside the accept's critical section, never across `.await`); before writing, the engine re-reads the ledger if its identity (`mtime`, `len`, inode) differs from what it last read or wrote, folds its in-memory overrides onto the fresh tree, and only then writes — so two instances accepting on one root serialise and neither loses the other's accepts. (c) A lock that cannot be taken within 2 s → `Refused::Busy` (new variant, text `another lastcall is writing <root>; try again`). Tests: two `Engine`s over one temp root accept different files → both accepts persist after both reopen; a lock held by a test thread → `Busy` within the timeout; stale index cleanup.
3. **Amendment A3 — relaxed hunk CAS (engine; worker 5a).** `accept_hunk` no longer calls `cas_live`; it keeps the baseline CAS (`BaselineMoved`), `NoSuchHunk`, and the deletion route (`rendered.oid.is_none()` → `accept_file`). The override stays `apply_hunks(base, hunks, [i])`: baseline plus the accepted hunk, nothing the user did not see. `Refused::Moved` remains for file/root/all accepts (A6 stays CAS). The status text for hunk accepts is unchanged. **Two proptests** in the unit tier on the private-store path (`ops.rs:1079` style, `TestRunner`, `RootKind::Draft`, no git): (i) *unseen edit survives* — accept hunk i after an edit elsewhere in the file: the recompute pile holds exactly that other edit and hunk i is gone; (ii) *in-place rewrite shows X→Y* — accept hunk X, rewrite X's lines to Y on disk: the recompute shows one pending hunk whose old side is X and new side is Y. Plus the reducer/e2e regression: `tui_accept_refused` keeps using `AcceptFile`. The §6.3 table row "accept hunk … refused if the file's live hash ≠ the rendered hash" is NOT yours to edit — report the exact replacement sentence as a proposed v1.5 line.
4. **The herdr task in the TUI (worker 5b).** `commands/tui.rs`: after `Engine::open`, resolve `[herdr] mode` (`off` → no task; `auto` → discovery failure is silent standalone; `on` → discovery failure is a visible standalone notice), `discover(env, pinned, probe)` → `Client::spawn(transport, ClientTimings::default(), ClientOptions { reconnect: true })` exactly as `hello_herdr.rs` does, and pass `(ClientHandle, rx)` into `run::run` as `Option<HerdrLink>`. `run.rs` gains a fourth `select!` arm over `rx` that maps `HerdrEvent` → `Action::Herdr(HerdrUpdate)` where `HerdrUpdate` is a **TUI-side, socket-free** enum (`Connected { version, protocol }`, `Standalone { reason }`, `Reconnecting`, `Roots(BTreeMap<PathBuf, RootAgents>)`, `Toast(Result<ToastShown, String>)`, `Focused(Result<(), String>)`) — the reducer never sees `Cache`, `PaneInfo` or the handle. The mapping (a pure function `herdr::derive(cache: &Cache, roots: &[RootMeta]) -> BTreeMap<PathBuf, RootAgents>` in `crates/lastcall/src/tui/herdr.rs`, unit-tested with hand-built caches) implements §6.6 association: walk each agent-bearing pane's `foreground_cwd` (fallback `cwd`) up to the deepest known root; unresolvable panes are ignored; per root `RootAgents { status: AgentStatus (max-attention rollup, priority blocked > done > working > idle > unknown), agents: u32, pane: Option<String> (the single agent's public pane id; the max-attention agent's when several) }`. Re-derive on `PaneAssociation`, `Resync`, `AgentStatusChanged`, `Connected` and on `Local::Roots` (roots changed under a live cache). `Disconnected` → `Reconnecting`; `Standalone` → `Standalone`. Quit order: `handle.shutdown()` before the watcher stops (bounded like the watcher's own timeout). Effects out of the reducer: `Effect::Focus(pane_id)` → `agent.focus` request on a one-shot connection (a new `wire::AGENT_FOCUS` const + `AgentTarget` params type + a `ClientHandle::focus(pane_id)` or a free function over the transport, whichever `hello_herdr` style fits; result → `HerdrUpdate::Focused`); `Effect::Toast(ToastRequest)` → deliverable 6.
5. **Reducer, render, input (worker 5b).** `App` gains `herdr: HerdrView { link: Link::{Off, Connected{version}, Reconnecting, Standalone{reason}}, roots: BTreeMap<PathBuf, RootFlag> }` where `RootFlag { status, agents, pane, ready: Option<Ready { acked: bool }> }`. **Rules:** a `done` rollup sets `ready` (`acked: false`) unless `ready` is already `Some` for this episode; an ack sets `acked: true` and leaves the dot dimmed while herdr still says `done`; any non-`done` rollup clears `ready` entirely (episode over) — so the next `done` re-alerts, but the same `done` never does. **Listing rule (§6.7):** a root appears in the nav when it has pending rows **or** an attention flag; attention = `ready` (any) or `blocked`. `working`/`idle`/`unknown` never list a root by themselves — they annotate roots already listed. **Nav root row:** the dot sits before the bold name: `⚑` bright for `ready` unacked, `⚑` dim for acked-still-done, `●` red `blocked`, `●` yellow `working`, nothing for `idle`, `·` dim `unknown`; with `agents > 1` the count follows the dot (`⚑2`). A flag-only root (no pending rows) shows the branch line and one dim row `nothing pending · agent done` so `Enter` has somewhere to land (the diff pane says the same). **Header:** right of the counts, `herdr 0.8.2` dim when connected, `herdr ⟳` when reconnecting, `standalone` dim when off/absent, `standalone: <reason>` when `mode = on` failed — `HitMap::HeaderHerdr`, click shows the full reason in the status line for 30 s. **Keys (ruling item 4; rec):** `d` = ack the selected root's flag (root row, or any row of that root, either pane), `g` = jump to agent (`Effect::Focus` on `pane`; status `focused <agent> in herdr`, or the error text), `w` = toggle workspace scope (deliverable 8); all three in `DEFAULT_KEYMAP`, `[keys]`-rebindable, described in `Action::describe` and the help overlay. **Mouse:** click on the dot = ack; click on the name = select (as today). **Hint ladder:** `d ack` and `g jump` are tier-1 hints shown only while the selected root has a flag; `w scope` is tier 2 and shows only when a scope is active. **Snapshot scenes** (pure reducer, fixed `HerdrUpdate`s, two `assert_snapshot!` each): `tui_herdr_status_dots` (three roots: working, blocked, done), `tui_herdr_ready_ack_dims` (before/after `d`), `tui_herdr_flag_only_root_listed`, `tui_herdr_header_states` (connected / reconnecting / standalone reason), `tui_herdr_scope_notice` (deliverable 8). Parity tests: `d` vs click on the dot; `g` by key vs a hit on a `HitMap::RootJump` if you add a control (optional; a key is enough). A `HerdrUpdate` arriving during the accept-all modal or the help overlay must not close them.
6. **Toasts (worker 5b).** Config: `[herdr] toast = true` (default; `false` never calls `notification.show`). On a root's `ready` transition, the loop starts (or extends) a coalescing timer `TOAST_DELAY` (rec 2 s, ruling item 5 — must exceed herdr's own toast lifetime read at the tag); when it fires, **one** `notification.show` names every root that went ready in the window: title `lastcall: <name> ready for review` or `lastcall: <N> repos ready for review`, body the names joined by `, ` (title ≤ 80 chars, body ≤ 240, sanitised per §5.9). Response handling: `shown` → status `toast shown`; `disabled`, `rate_limited`, `busy`, `no_foreground_client`, transport error → drop silently (debug log), never retry. Acked-before-the-timer roots are dropped from the toast. G7 as a unit test on the mock: `status_events` working→done, `canned("notification.show", busy)` at first then `shown` — assert exactly one request per burst, arriving no earlier than `TOAST_DELAY` after the done event (use `MockControl::requests()` timestamps or a test clock), and with `canned(…, disabled)` exactly one request and no second. Docs ship herdr's required `[toast] delivery = "herdr"` snippet.
7. **Worktree events drive the watch set (worker 5b).** `HerdrEvent::WorktreeChanged { Created | Opened | Removed, path, .. }` → after a 500 ms debounce, `blocking(&engine, |e| e.rescan())` and the resulting `RootsChanged` flows through the existing `Local::Roots` path (the engine's own `roots::discover` decides; the event is only a trigger — a `Removed` path still on disk stays a root). Test on the mock: push `worktree.created` for a linked worktree the test just made under the temp parent → the root appears within ≈1 s of the event, not at the 30 s rescan (assert on `Local::Roots` / the PTY frame with the rescan backstop set long via `--poll`).
8. **Workspace-scoped roots (worker 5b; ruling item 1).** With `env.var("HERDR_WORKSPACE_ID")` set and the cache holding that workspace: if `worktree.checkout_path` is `Some`, scope = the root whose path equals it (canonicalised) plus every root whose `badge` links it (`WorktreeOf(p)` / `NestedIn(p)` with `p` = that root); else scope = every known root containing the cwd (`foreground_cwd`, fallback `cwd`) of any pane in that workspace; else no scope. Scoped-out roots are hidden from the nav and the header counts; the status bar shows `scope: <workspace label> · <N> repos hidden (w shows all)` while active; `w` toggles between scoped and all for the session; `[herdr] scope = "workspace" | "all"` sets the start state (default `workspace`). Re-derive scope on the same triggers as deliverable 4. Standalone or no env var → no scope, no notice. Accept-all under scope covers listed roots only (it already folds "every listed root"). Unit tests on `derive`-level functions with hand-built caches; the `tui_herdr_scope_notice` snapshot.
9. **§11 hardening — title-only `pane_updated` (engine `herdr` client; worker 5b).** A `pane_updated` whose payload differs from the cached `PaneInfo` only in `title`, `terminal_title`, `terminal_title_stripped`, `revision`, `scroll` or `tokens` updates the cache and emits nothing and schedules no resync; any other delta keeps today's behaviour. Test `client_ignores_title_only_pane_updated` (two title-only events → zero snapshots; a `cwd` change → the usual `PaneAssociation` + resync). Report closes the §11 entry.
10. **Real-server integration tests (worker 5c; `just test-integration-herdr`, pinned binary via `LASTCALL_TEST_HERDR_BIN`, self-skip with a printed reason when unset).** In `test_integration_herdr_real.rs` beside the Phase 1 test, driving the `Client` headlessly (no PTY): **G3** `herdr_real_done_flip_heals_within_fallback` — drive a pane to `done` the way the Phase 1 test derives it, then focus the tab through the API (`tab.focus`, add the wire const) with the lifecycle stream **deprived of the flip** (`ClientTimings { fallback: 3 s }`, and the lifecycle connection closed by the test before the focus): assert an `AgentStatusChanged { to: idle }` or a `Resync` carrying idle within the fallback, never a stale `done` past it. **G6** `herdr_real_disconnect_reconnect_converges` — stop the server (`herdr server stop` or kill the pid the harness started), assert `Disconnected`, restart it with the same socket path, assert `Connected` + the bootstrap sequence + a `snapshot()` equal to a fresh `session.snapshot`. The CI integration job already fetches the pinned binary; both tests run there (macOS non-blocking as today).
11. **Scheduled herdr-compat workflow (worker 5c; §7.5, gate item 8).** (a) **Consumed-surface fixture** `crates/lastcall-testkit/fixtures/herdr/schema/consumed-surface.json`: a projection of `herdr api schema --json` limited to what we consume — the methods `ping`, `session.snapshot`, `pane.get`, `pane.list`, `worktree.list`, `events.subscribe`, `notification.show`, `agent.focus`, `workspace.get`, `tab.focus` (params and result schemas), the §5.4 event set plus `pane.agent_status_changed` (payload schemas), and the `AgentStatus` enum — generated by `just herdr-schema-fixture` from the pinned binary (a small Rust bin or test in the testkit that reads the JSON and writes the projection deterministically: sorted keys, `\n` line ends), committed with a `consumed-surface.json.provenance.md` (the fixture dir's convention) naming the tag. (b) **Test** `herdr_real_schema_consumed_surface_unchanged` (real-server subset): runs `<bin> api schema --json`, projects, compares to the fixture, and prints a readable JSON diff on mismatch. (c) **Workflow** `.github/workflows/herdr-compat.yml`: triggers `schedule` (weekly, Monday 06:00 UTC), `workflow_dispatch`, and `pull_request` on paths `[.github/workflows/herdr-compat.yml, crates/lastcall-testkit/fixtures/herdr/schema/**, justfile]` — the PR trigger produces the "has run green at least once" link before merge. Job (ubuntu): `just herdr-fetch-latest` (new recipe: `gh release download` of the **latest** release, not the pinned tag; prints the version), `just test-integration-herdr-latest` (the `herdr_real_*` subset against that binary, with the protocol guard's refusal counting as drift), then on failure `gh issue create --label herdr-compat --title "herdr compat drift: <version>"` unless an open issue with that title exists (`gh issue list --search`), with the job log link in the body; `permissions: issues: write, contents: read`, `GH_TOKEN: ${{ github.token }}`. `ci.yml` is untouched.
12. **Docs and report (worker 5c).** `docs/dev/tui.md` (a "herdr in the UI" section: dots, flag, ack/jump/scope keys, header states, toast config and the herdr snippet, the demo recipe for the sponsor gate: two panes, an agent finishing on the left, the flag on the right); `docs/dev/engine.md` (parallel open/scan and the pool bound, the allowlist additions, the per-process index and ledger lock, `Refused::Busy`); `docs/dev/testing.md` (new tests and recipes, the real-server subset, the compat workflow, updated tallies); `docs/dev/bench.md` (deliverable 1d); README/config reference for `[herdr] toast` and `[herdr] scope`. Then the Gate 5 self-assessment and final report.
13. **Dependencies:** none new expected. Locks via `std::fs::File`; JSON via `serde_json`; the workflow uses `gh` from the runner image. Any addition is reported with the reason.

## Traps (each is a verified failure elsewhere; design them out)

- **Parallel open that changes state.** Path-ordered insertion after the join; `scan_seq` assigned in the serial apply step only. The golden and the fixed-pool determinism test catch a reorder; a verifier will grep for `scan_seq` writes outside the apply step.
- **Holding the engine mutex across the pool or across `.await`** — the Phase 3/4 trap again. The pool lives inside one synchronous `rescan`/`scan_all` call under one `blocking`.
- **`config --list` semantics.** Values may contain `=` and newlines; `-z` framing is `key\nvalue\0`. Multi-valued keys repeat; take the last, as `config --get` does. An `includeIf` that git resolves for `--get` also resolves for `--list` (same process, same cwd) — do not "optimise" by reading the file yourself.
- **`rev-parse --abbrev-ref HEAD` on a detached HEAD prints `HEAD`**, and `--is-shallow-repository` prints `true`/`false` on its own line; the parser must pair outputs by position, and the branch-name path must not turn `HEAD` into a branch called HEAD (today's `symbolic-ref -q` exit code carries that).
- **The bench in debug** measures nothing (Phase 4 trap): `just bench` builds release and skips otherwise. Fixture construction stays outside the timed region. Run S1 before and after on the same machine and paste both machine blocks.
- **Sick FSEvents on the sponsor's Mac** (§11): fixed on 2026-09-03 by restarting `fseventsd`, but the bench still records which path it measured, and the worktree-event test asserts on the herdr trigger, not on a file event.
- **Stale flags from the v0.8.2 replay.** Every connect replays up to 512 lifecycle events; the client coalesces them into snapshots, and the *snapshot* is truth. Derive `RootAgents` from the cache after `Resync`/`PaneAssociation`, never from individual replayed `Lifecycle` events, or the nav flashes flags for panes that died a day ago.
- **`done` re-alerting after ack.** The rule is per episode: `ready` clears only on a non-`done` rollup. A resync that re-reports the same `done` must not reset `acked`. The `tui_herdr_ready_ack_dims` second frame and a reducer test with a repeated `done` update pin it.
- **Toast retry loops** — §5.9: `busy`/`rate_limited` are "drop it". One request per burst; the mock's request count is the proof.
- **A toast that names a root the user already acked** — filter at fire time, not at schedule time.
- **The reducer touching a socket.** `app.rs` never imports `lastcall_engine::herdr`; `HerdrUpdate` is the boundary (grep in the gate).
- **`agent.focus` with the wrong target.** Pass the snapshot's `pane_id` verbatim (public id); a name would be ambiguous with two agents of the same kind. `agent_not_found` (pane died) is a status line, not a fatal.
- **Scope hiding pending work silently.** The scope notice with the hidden count is mandatory while a scope is active; the header counts follow the scope; `w` always restores the full view. A workspace whose `worktree.checkout_path` names a directory that is not a lastcall root (a repo outside `parent_dirs`) yields no scope, not an empty nav.
- **Two instances, one ledger.** The lock is per accept, the re-read is by identity, the fold is onto the fresh tree. A test with two engines is the proof; "it's a rename so it's atomic" is the wrong argument (atomic rename does not prevent lost updates).
- **pid reuse for stale index cleanup** — check liveness with `kill(pid, 0)`-style probing on Unix; on Windows skip cleanup rather than delete a live process's file.
- **`herdr server stop` semantics in the G6 test** — the harness that starts the pinned server owns its pid; restart on the **same** socket path or the client's discovery pins the old one. If the real-server tests cannot restart a server hermetically, say so and make G6 a mock test plus a manual recipe, reported as a deviation.
- **Fixture generated from master.** The consumed-surface fixture comes from the pinned release binary (`just herdr-fetch`), never from the reference clone; the provenance file names the tag.
- **Real tests that skip silently.** Each real-server test prints `SKIP <name>: LASTCALL_TEST_HERDR_BIN unset` exactly like the Phase 1 test; the CI integration job must show them running.
- **Tests touching the real herdr** (`~/.config/herdr/`, the default socket) — every test injects `Env` with its own `HOME`/`XDG_CONFIG_HOME`/`HERDR_SOCKET_PATH`; the discovery unit tests already show the pattern. **The reference clone `~/dev/git/herdr` is read-only; `git push` never; `git add -A` / `git add .` are denied; `z_ignore/` and other worktrees are not yours.**

## Gate 5 (closes this phase; evidence forms are what will be accepted)

- [ ] **Entry obligation:** `bench.md` before/after tables for S1 and the new S1h with both machine blocks; `BENCH` stderr lines pasted; S1h `first_frame_ms` ≤ 6,000; S1 `first_frame_ms` ≤ 14,142 (¼ of 56,568); spawn counts per root at open and per scan pasted; the fixed-pool determinism test and the golden green.
- [ ] **[sponsor]** Agent finishes in the left pane → repo flags in the right pane (and with `toast.delivery = "herdr"` a toast): the demo recipe in `tui.md` pasted; the sponsor runs it on the real session.
- [ ] **Done-flip staleness healed:** `herdr_real_done_flip_heals_within_fallback` green in `just test-integration-herdr` with its event trace pasted.
- [ ] **Disconnect / reconnect converges:** `herdr_real_disconnect_reconnect_converges` green with the trace pasted (or the documented deviation from the Traps list, plus the mock version).
- [ ] **Orphan status connections reconciled:** `client_tears_down_orphan_status_subscription_at_resync` (existing) named and green.
- [ ] **Mock-server unit tests:** the eleven existing names above plus `client_ignores_title_only_pane_updated`, the G7 toast tests, and the ack-episode reducer tests, all named.
- [ ] **§5 re-verification** against v0.8.2 for `agent.focus`, `workspace.get`, `tab.focus` and the pane env: the `gh api` paths read and the one-line result per surface, pasted (the orchestrator's kickoff-time check is in this file).
- [ ] **Compat workflow live:** `herdr-compat.yml` committed; the fixture and its provenance committed; `herdr_real_schema_consumed_surface_unchanged` green locally; the PR's `pull_request`-triggered run link green (the sponsor pushes; you paste the expected job name and the command `gh run list --workflow herdr-compat.yml`).
- [ ] **UI surfaces:** the five snapshot scene names with the `tui_herdr_status_dots` and `tui_herdr_ready_ack_dims` frames pasted; a PTY scene `pty_herdr_flag_ack_jump` against the mock (working→done → `⚑` in the frame → `d` dims it → `g` → the mock's request log holds `agent.focus {"target": "<pane_id>"}`) with its timing lines.
- [ ] **A3:** the two proptest names green in `just test-unit` and the proposed §6.3 sentence in the report.
- [ ] **N instances:** the two-engine accept test and the `Busy` test named and green.
- [ ] **Floor:** final unit count per crate pasted (≥ 318 at every commit; this number is the Phase 6 floor). Expect the engine to gain ≥ 15 and the binary crate ≥ 20.
- [ ] **Greps, pasted:** `rg -n 'Command::new\("git"\)' crates` → only `git.rs`; `rg -n 'lock\(' crates/lastcall/src/tui` → none; `rg -n 'std::env::var|home_dir\(' crates/lastcall/src` → only the `LASTCALL_LOG*` reads; `rg -n 'lastcall_engine::herdr' crates/lastcall/src/tui/app.rs` → none; `rg -n 'scan_seq' crates/lastcall-engine/src/engine.rs` → only the apply step and the accessor; `rg -n 'notification.show|NOTIFICATION_SHOW' crates/lastcall/src` → one call site.
- [ ] **Built-artifact pass:** `cargo build --release`; `just probe-tui` once with `LASTCALL_TEST_HERDR_BIN` unset (standalone badge, nothing else changed) and once against `just mock-herdr` with a `status_working_to_done` script (the flag appears; `d`; `g`; quit clean) — one paragraph each; `./target/release/lastcall status --json` on the golden fixture unchanged.
- [ ] **Standing:** `just lint` clean; `just test-prepush` green before the report; no test weakened or deleted; unit tier deterministic (no git subprocesses in the binary crate's unit tests; no sleeps > 50 ms; no sockets except the in-test mock); `cargo check -p lastcall-engine --no-default-features` green; dependencies exactly deliverable 13.

## Operational rules

- **Sacred and untouchable:** the sponsor's real state dir (`~/.local/state/lastcall`), config (`~/.config/lastcall`, `~/.config/herdr`), the default herdr socket, every repo under `~/dev/git/` other than your worktree (the reference clones `~/dev/git/herdr` and `~/dev/git/drydock` are read-only, never built), and `z_ignore/`. Every test injects `Env` and its own `LASTCALL_STATE_DIR`; the bench builds everything under a `TempDir`.
- **Frozen artifacts (STOP, human-only):** `00-spec.md` §5 and §6, `01-scenarios.md`, this kickoff. A behaviour you need that they contradict → build the spec's form, report the alternative as an open question with the exact sentence you would change.
- **Spend and context:** no LLM calls in the product. Per worker: expected ≈ $1–2, flag above $3, hard tripwire $5 — stop, commit clean, report. Re-read this file rather than guessing a deliverable's wording.
- **Test tiers and push (§10 Phase 4 close-out):** `just test-unit` on every commit (the pre-commit hook runs lint + unit); `just test-prepush` (integration tier + 64-case proptests) before your final report, pasted. `git push` is the sponsor's; the pre-push hook is the backstop, not your signal.
- **Worker split (ruling item 3; rec three sequential workers, the Phase 4 shape):**
  - **5a — engine:** deliverables 1, 2, 3 with their tests and the bench re-run. Exit evidence: the S1/S1h numbers, spawn counts, the proptest names, the two-engine test, unit count.
  - **5b — herdr in the TUI:** deliverables 4, 5, 6, 7, 8, 9. Exit evidence: snapshot names and the two frames, the mock tests named, the PTY scene, unit count.
  - **5c — real-server tests, compat workflow, docs, report:** deliverables 10, 11, 12, 13 check, the Gate 5 self-assessment and the final report for the whole phase.
  Each worker reads the whole kickoff, builds only its scope, and leaves the seams the next one needs exactly as named (`HerdrLink`, `HerdrUpdate`, `RootAgents`, `wire::AGENT_FOCUS`/`TAB_FOCUS`). Verifier (a) after 5a, (b) after 5b, (c) before 5c's final report.
- **Commit discipline:** conventional commits, first line < 72 chars, phase-shaped: config parser → batched rev-parse → parallel open → parallel scan → bench + `bench.md` → per-process index → ledger lock → A3 + proptests → `HerdrLink` + `HerdrUpdate` plumbing → `derive` + reducer → render + snapshots → input + parity → toasts → worktree trigger → scope → title-only hardening → real-server tests → fixture + schema test → workflow → docs. Each commit green under the pre-commit hook.
- **Never end your turn to wait for anything.** No notification will come. Poll bounded cycles in-turn. No sanctioned mid-phase checkpoint; your next turn-end after starting is the final report.

## Working agreements

- Grounded claims only: test names and counts, `BENCH` lines, pasted frames, request logs from `MockControl`, `gh api` paths. "Looks right" is not evidence.
- Fresh-context **verifier subagents** (adversarial charter) at three milestones; findings and resolutions go in the report:
  - **(a) after 5a.** Hunts: a `scan_seq` assigned inside the pool; state that differs between parallelism 1 and 8 (run the golden both ways); a lock held across the pool; `config --list` parsing that mishandles `=`/newline values or multi-valued keys; a detached HEAD or linked worktree that the batched `rev-parse` misreads; bench numbers from a debug build or a different machine than the baseline; a lost update with two engines (write the test if the worker's is weak); `index.<pid>.tmp` left behind; a hunk accept that still calls `cas_live`; a proptest that never exercises the in-place rewrite.
  - **(b) after 5b.** Hunts: `app.rs` reaching into the client crate; a flag that re-alerts after ack on a repeated `done`; a flag derived from a replayed lifecycle event instead of the cache; a stale flag surviving reconnect; two `notification.show` requests for one burst; `agent.focus` sent with a name or a non-public id; a scope that hides pending rows without the notice; the accept-all modal or help overlay closed by a `HerdrUpdate`; the quit path hanging on `handle.shutdown()`; a `HerdrUpdate` arriving before `sync_roots` and being dropped on the floor.
  - **(c) before the final report.** Hunts: a real-server test that passes while skipping; a fixture generated from master or by hand; a workflow YAML that cannot run (`gh workflow view` on the branch, `actionlint` if available); an issue-creation step that spams (no dedupe); docs that name a key the code does not read; `bench.md` lines that do not match the pasted stderr; the Gate 5 table claiming an item without its named evidence.
- Stop conditions: gate met; spend tripwire; a frozen-artifact edit becomes tempting; a genuine human-only blocker (e.g. the real server cannot be restarted hermetically — document and continue with the mock form). Commit clean, document, stop.

## Final report

Gate 5 self-assessment, one row per gate item with the evidence pasted. Then: unit count per crate and total (the Phase 6 floor), the snapshot and PTY scene names, the `BENCH` lines and both machine blocks, spawn counts, verifier findings with resolutions, the `gh api` verification lines, built-artifact outputs, tool versions, proposed spec additions (v1.5: §6.3 hunk-accept sentence; §6.1 config keys `herdr.toast`, `herdr.scope`; §11 entries closed: serial open/scan, shared `index.tmp`, title-only `pane_updated`), and every deviation from this kickoff with its reason.

---

## Rulings (presented after the design review; sponsor verdicts pending)

1. **Workspace scope rule and default.** Rec: scope by provenance first (`worktree.checkout_path` → that root plus its linked worktrees / nested repos), then by pane cwd containment, else none; default on (`[herdr] scope = "workspace"`), `w` toggles for the session, and a mandatory `scope: … · N repos hidden` notice — a lastcall pane in a one-repo workspace should show that repo (sponsor direction 2026-09-02), and the notice plus toggle keep invariant 2 ("never hides" is satisfied by one keypress and a visible count). Alt: no filtering, only ordering — the workspace's roots sort first and are marked; strictly never hides anything, but the sponsor's 50-clone parent dir still fills the nav in every workspace pane, which was the complaint. Two-way door (a rule and a default).
2. **Several lastcall processes over one state dir.** Rec: graceful N — per-process temp index, an advisory lock per root around each accept's read-modify-write, and a re-read-before-write by file identity, with `Refused::Busy` after 2 s; item 1 makes one lastcall per workspace pane the normal setup, so a single-instance lock would fight the feature. Alt: one instance per state dir (`<state>/lastcall.lock`, the second instance exits with `lastcall already running (pid N)`); a day's work less and no lock in the accept path, right if the sponsor runs exactly one lastcall and item 1's scoping is taken as "ordering only". Two-way door, but the lock format is a storage detail that the §6.1 layout would record.
3. **Worker split.** Rec: three sequential workers (5a engine, 5b herdr UI, 5c real-server + workflow + docs), the Phase 4 shape, because the entry obligation is a self-contained engine change with its own instrument and the herdr UI is the bulk. Alt: two (5a engine, 5bc everything else) — one fewer merge and verifier pass; fine if the compat workflow is judged small, but 5c also holds the two real-server tests whose hermetic restart may need a deviation ruling, better isolated from the UI worker. Operational.
4. **Keys and the listing rule for annotations.** Rec: `d` ack, `g` jump-to-agent, `w` scope toggle (all unbound today, all `[keys]`-rebindable); click on the dot acks; attention = `done` (acked or not) or `blocked` lists a root with no pending rows, `working` only annotates. Alt: `Enter` on a flag-only root jumps (no `g`), and `blocked` does not list a root — fewer keys; but `Enter` already means open, and a blocked agent in a repo with nothing pending is exactly the case the user cannot see from the pile. Two-way door.
5. **Toast default, delay, coalescing.** Rec: `[herdr] toast = true`, `TOAST_DELAY` 2 s (or herdr's own toast lifetime + 500 ms if that is longer at the tag), one toast per window naming every root that went ready; herdr's `delivery = off` default already makes it opt-in on herdr's side, so our default can be on. Alt: default `false` and no delay logic (a plain one-shot at the `done` instant) — simplest, but §5.9 verified that the same-tick call returns `busy` under `delivery = herdr`, so the feature would never fire for the one user who enabled it. Two-way door.
6. **Parallelism bound and the allowlist widening.** Rec: `EngineOptions.parallelism` default `min(available_parallelism, 8)` (no config key in v1), and the `RepoGit` allowlist widens by exactly `config --list -z` and multi-flag `rev-parse` (with `--git-path`), documented in `engine.md`; git spawns are I/O-bound so 8 saturates a laptop without thrashing 100 roots' stores. Alt: a `[engine] parallelism` config key and no allowlist change (keep per-key `config --get`, parallelise only) — reaches the ≥ 4× S1 target by parallelism alone on an 8-core machine but not the 6 s S1h target on a 4-core one, and adds a knob nobody asked for. Two-way door.
7. **§3.5 circle-back after Phase 4 (ack semantics; accept-all confirmation threshold).** Rec: no change — local ack is what this phase builds (G0 Q3; herdr has no mark-seen API, re-verified at v0.8.2), and the >10-file confirm was used in the Gate 4 sponsor run without complaint. Alt: lower the confirm threshold to 5 or make it a config key — only if the sponsor found the modal too rare or too frequent in real use. Two-way door; recorded either way in §10.
8. **Claude Design pass timing and inputs.** Rec: after Phase 5 merges, on the real binary with herdr connected — the pass receives the Gate 4 sponsor-run rulings, this phase's five snapshot scenes, the header states and the scope notice, and its findings become the Phase 6 kickoff's first agenda item (post-build rulings, two-way doors). Alt: a sanctioned mid-phase checkpoint after 5b (the scenes exist, docs not yet written) — catches visual changes before 5c documents them, at the cost of a stall-shaped pause in the phase; right if the sponsor expects large layout changes rather than glyph/colour/copy changes. Operational.
